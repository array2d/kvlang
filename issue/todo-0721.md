# todo-0721: /lib 树设计负债修复 ✅

> 来源：deep-dive §11 debugger 实战分析 + 代码审计 + kvspace 数据模式分析
> 验证：PASS:100 FAIL:0 (tutorial/test.py)

## P1: 类型/数据正确性 ✅

- [x] **`slotValue` kind `"int"` → `"int64"`** — `layoutrwir.go`
- [x] **`/src/` 冗余写入删除** — 移除 `WriteFunc` 中 `/src/` 写入
- [x] **`keytree.Src()` + `src.go` 删除** — 整个文件删除
- [x] **`keytree.LibSrcMap()` 删除** — entry.go 函数 + cmd/layoutrwir.go 写入 + srcMap 变量
- [x] **`ast.Flat()` 重复 `if v == "return"`** — 删除重复分支
- [x] **`maxParams=10` → 128 + 截断报错** — 对齐 C 标准 ≥127，超出返回 error（`op/instruction.go`）
- [x] **`_litNN` → `._litN`** — 引擎保留前缀隔离用户变量空间

## P2: 代码质量 ✅

- [x] **layoutrwir.go 包注释更新** — `/src/` + `/func/` → `/lib/`
- [x] **`ChildFrameRoot` 删除** — frame.go 函数删除，调用处(`HandleCall`)内联为 `pc`
- [x] **`IsControlOp` 清理死 opcode** — 移除 OpIf/OpFor
- [x] **`extractAddr0` 去重** — `op/pc.go` 导出 `ExtractAddr0` + `ExtractAddr0FromPC`，`layoutrwir.go` 调用之，移除本地实现 + `strconv` import
- [~] **`.rparam`/`.wparam` 解析去重** — 分析后跳过：两处语义不同（路径 vs 值），且 key 构造已统一在 `keytree.RParam/WParam`

## P3: 文档同步 ✅

- [x] **deep-dive §9.1 kind 禁止别名铁律** — 写入"禁止 kind 别名"规则
- [x] **keytree DESIGN.md 全面更新** — 路径全景树 `/func/`+`/src/` → `/lib/`；链接模型 `_fn` → `.funclib`；帧元数据 `.callpc`/`.ret0` → `.rparam`/`.wparam`
- [x] **deep-dive §6 HandleCall/Return 伪代码更新** — `FuncIdx` → `kv.Get(LibIdx)`；`ChildFrameRoot` → `callPC`；乱码行修复

## 后续 tothink（不在今日范围）

- [ ] P0: `runLib` 索引查找断裂 — `kvlang run pkg.func` 不工作
- [ ] P0: `/lib/idx/` 扁平命名覆盖 — 同名函数不同包冲突
- [ ] `/lib/<name>` 节点双重身份是否需要显式分离
- [ ] Block label 嵌入 `/` 分隔符是否应改用 `.` 拼接
