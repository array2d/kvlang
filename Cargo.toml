[package]
name = "kvlang"
version = "0.1.0"
edition = "2021"
description = "kvlang VM — Rust runtime (kvcpu executor)"
license = "MIT"

[lib]
path = "lib.rs"

[[bin]]
name = "kvlang"
path = "cmd/kvlang/main.rs"

[dependencies]
libc = "0.2"
