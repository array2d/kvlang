// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/语法三职能 {
	r##"# 为什么同一语法同时承担三种职能

「VM 指令 / 高级语言 / 编译器 IR 三者合一」是 kvlang 设计哲学的集中体现，而非偶然。

传统上，高级语言源码经编译器降低为 IR，IR 再编译为机器码，三层各有独立表示，不可互换。kvlang 的扩展编译器（作为 rwir 扩展）把 rwfunc 融合成单条 rwir——融合后的 rwir 仍然以同样的 kvspace 路径存在，可被同样的 `kvspace tree` 观察，可被同样的 runtime 解释。没有独立的 IR 格式，也没有独立的二进制格式。

这个设计让"算子融合"成为 kvlang 成为训推引擎前端的关键机制，而不是一个需要独立工具链的编译器优化流水线。
"## -> /lib/kvlang/规范/设计理由/语法三职能
}
