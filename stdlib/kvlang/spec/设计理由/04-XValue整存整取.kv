// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/XValue整存整取 {
	r##"# 为什么 XValue 必须整存整取

整存整取铁律的动机是消除并发 RMW（Read-Modify-Write）竞态。若允许按字节偏移部分写 XValue.raw，则任意两个 worker 并发写同一 slot 的不同字节区间时，结果是未定义的——kvspace 无法在协议层保证字节级的原子性。

将原子单位上移到 XValue（单 key 的原子替换），每次读写即单 key 的完整替换，竞态被消除，同时与"元存全局公有、任意节点可读写"的定位保持一致：节点间协作只需约定 key，而不需要协商字节区间边界。
"## -> /lib/kvlang/规范/设计理由/XValue整存整取
}
