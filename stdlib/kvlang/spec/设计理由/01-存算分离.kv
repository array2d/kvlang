// 欢迎加入kvspace世界
lib kvlang/spec/设计理由/存算分离 {
	r####"# 设计理由：存算分离

## 为什么要三分：存储、计算、控制流严格独立

传统 VM 把存储（栈+堆+全局段）、计算（ALU 指令内联寻址）、控制流（整数 PC）混在同一内存空间里。这个
设计对单机批量执行是高效的，但带来三个顽疾：

1. **崩溃不可恢复**：PC 是整数，崩溃后进程消失，执行状态无法独立持久化。
2. **调试需要附着进程**：状态在私有内存，必须 attach 调试器才能观察。
3. **算子与调度器深度耦合**：计算与控制流混在同一线程，难以独立替换或分布式拆分。

kvlang 的回答是：**把存储提升到 kvspace（独立于进程的 KV 树）**，计算（op-plat）与控制流（kvcpu）都
成为无状态的执行者，只消费或导航存储层，自身不持有状态。

三分之后：崩溃恢复只需读回 PC 路径（已存于 KV）；调试只需 `kvspace tree`，无需 attach；算子可独立替
换，调度层可分布式拆分。

## 为什么元存与扩展存储要再分两层

存储层内部还有一道分裂：元存（kvspace 核心，管理 slot/索引/指令树/帧栈）与扩展存储（tensor data，在
GPU 显存或本地 shm）。

这道分裂的动机是：

- **元存全局公有**：所有 worker 连同一 kvspace 即可共享代码（`/lib/`）与执行状态（`/vthread/`），不
  需要独立调度器元数据中心。这是分布式协作的基础。
- **扩展存储连接者私有**：GPU 显存本质上只有直连进程可访问，强行放入元存既无法实现又带来巨大序列化开
  销。tensor 数据由 heap-plat / op-plat 直接管理，跨卡搬运（AllReduce 等）在算子层显式处理。

铁律由此自然推出：kvspace 只存 tensor 元数据与扩展存储句柄，不存 tensor 原始数据。

## 为什么 XValue 必须整存整取

整存整取铁律的动机是消除并发 RMW（Read-Modify-Write）竞态。若允许按字节偏移部分写 XValue.raw，则任意
两个 worker 并发写同一 slot 的不同字节区间时，结果是未定义的——kvspace 无法在协议层保证字节级的原子
性。

将原子单位上移到 XValue（单 key 的原子替换），每次读写即单 key 的完整替换，竞态被消除，同时与"元存全
局公有、任意节点可读写"的定位保持一致：节点间协作只需约定 key，而不需要协商字节区间边界。
	"#### -> /lib/kvlang/spec/设计理由/存算分离
}
