// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/拒绝折中妥协 {
	r##"# 拒绝折中妥协

语言设计中最危险的决策是"两个对立方案各取一半"。折中表面上满足了两方，实际上两方都没满足，还引入了边界处的未定义行为。

kvlang 在几个重要决策上主动选择了其中一边：
- 不允许用户自定义数据结构（而非"可选地允许"）。
- layout 是 o0 编译器：做检查、布局与降级，不做任何优化（而非"顺手做点优化"）；优化一律留给扩展编译器，以免丢失高层语义。
- 扩展编译器完全在扩展里，不在核心里（而非"核心内置一个简单编译器，扩展可替换"）。

每个"不允许"背后都有一个"如果允许会打破什么"的具体分析，而不是偏好或审美。
"## -> /lib/kvlang/规范/设计理由/拒绝折中妥协
}
