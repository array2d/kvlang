// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/键族机制 {
	r##"# 为什么选择键族作为唯一数据结构机制

键族（key family，`base·field`、`base·i`）是 kvspace 路径天然支持的组合方式。把"数据结构"降格为"键的命名约定"，有几个好处：

- **无独立类型注册**：键族的结构由 rwfunc 的读写模式决定，不需要预先声明类型定义。
- **跨节点天然共享**：任何连接到同一 kvspace 的节点都能按同样的命名约定访问同一份数据，无需序列化/反序列化。
- **调试透明**：`kvspace tree base·` 直接列出该数据结构的所有字段，无需类型系统辅助。

代价是：kvlang 无法在 layout 阶段对"结构完整性"做静态检查——rustc 能检查 struct 字段必须全部初始化，kvlang 不能。这是已知取舍，kvlang 以 runtime 报错替代静态检查。
"## -> /lib/kvlang/规范/设计理由/键族机制
}
