// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/无取址运算符 {
	r##"# 为什么没有取址运算符

传统语言的 `&` 取址产生的是私有内存地址，只在当前进程内有意义，且与内存布局深度绑定。kvlang 的"变量名"本身就是该变量的 kvspace 路径——路径在任何连接到同一 kvspace 的节点上都有意义，不需要"取址"这个概念，也不存在私有地址。
"## -> /lib/kvlang/规范/设计理由/无取址运算符
}
