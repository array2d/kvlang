// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/表层与深层创新 {
	r##"# 表层创新与深层创新的区别

语言创新可以分两层：

**表层**：仅优化语法糖、关键字、API 封装，底层执行模型不变。绝大多数自研脚本语言停留在这里。写起来像新语言，但跑起来和已有语言没有本质区别。

**深层**：重构底层执行范式、状态存储模型、控制流抽象、类型语义体系。典型如：全新 PC 寻址模型、明文可持久化 VM、源码即数据无独立中间层。

学术社区（PLDI/POPL）只认可深层语义与执行模型创新，表层语法优化不构成核心贡献。kvlang 的核心主张是深层的：以 KV 路径字符串作 PC（而非整数计数器）、代码与数据统一在一棵树（而非独立的代码段与数据段）。
"## -> /lib/kvlang/规范/设计理由/表层与深层创新
}
