// 欢迎加入kvspace世界
lib kvlang/spec/设计理由/一切皆明文 {
	r####"# 设计理由：一切皆明文

## 为什么选择明文而非字节码

传统 VM 以字节码为核心：PC 是整数偏移、opcode 是单字节枚举、类型在编译期丢失、运行时状态只有调试器能
读。这是为解释执行速度而做的取舍——紧凑的二进制表示减少内存占用和取指开销。

kvlang 反向选择：**一切关键信息以明文字符串存于 kvspace 树**。动机是透明性：

- **对人透明**：`kvspace tree` 命令不需要反汇编器，直接读出代码、数据、执行状态的当前快照。
- **对 agent 透明**：agent 用同一套 `kv.Get` / `kv.Set` API 读写，程序自省与 agent 协作共享同一份数
  据，无需单独设计"可观测性接口"。

这个选择有直接代价：PC 是字符串而非整数、opcode 是 `"+"` 而非单字节——字符串比较和路径解析的开销显著
大于整数操作。

## 为什么代价可以接受

代价可以接受，因为 kvlang 的定位是**调度层**而非计算层。

最耗时的操作是 tensor 计算，这部分不在 kvlang 内执行——op-gpu 将 rwfunc 编译为 GPU kernel（TileLang
AOT → `.so` → dlopen），由 GPU 硬件加速。kvlang runtime 只做调度：判断走哪条路径、调用哪个 rwir（经
def rwir 路由）、读写哪个 slot。调度层的绝对耗时在 tensor 计算面前可以忽略。

透明性在调度层兑现，算力在扩展引擎层找回——这是 kvlang 选择明文的根本底气。

## 为什么 PC 是字符串而非整数

整数 PC 只能表示"当前在哪条指令"，字符串 PC 能表示"当前在调用栈的哪条路径上"——路径深度即栈深度，路径
本身即调用链。

字符串 PC 直接带来崩溃恢复能力：进程崩溃后，PC 路径已持久化在 kvspace，重启后从该路径继续执行，无需
重放或检查点协议。整数 PC 做不到这点，因为整数脱离了进程上下文就失去意义。

## 为什么 `&` 是绝对地址运算符而非取址运算符

传统语言的 `&` 取址产生的是私有内存地址，只在当前进程内有意义，且与内存布局深度绑定。kvlang 的"变量
名"本身就是该变量的 kvspace 路径——路径在任何连接到同一 kvspace 的节点上都有意义。故 kvlang 保留 `&x`
（`≡ kv·abs(x)`），但它的产物是**指向该节点的 Ptr**（ref=1，body=绝对路径）、不是私有地址：没有进程私
有地址空间，也没有 C 式取址的内存布局含义，故规范称它为**绝对地址运算符**（见 [[寻址与命名]]、
[[运算符基础]]）。
	"#### -> /lib/kvlang/spec/设计理由/一切皆明文
}
