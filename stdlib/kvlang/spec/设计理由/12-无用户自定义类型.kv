// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/无用户自定义类型 {
	r##"# 为什么不允许用户自定义数据结构

用户自定义数据结构（class/struct/typedef）的本质是：在语言的类型系统之上再建一层私有命名空间，描述"这些字节如何解释"。这在私有内存模型下有必要，因为内存中没有自描述能力。

kvlang 的存储层（kvspace）是自描述的：每个 XValue 携带 kindexpr（类型签名），路径携带层次语义（`/`、`·`、下标）。在这个基础上，"如何组合键"已经足以表达所有数据结构，不需要额外的类型构造语法。

强行引入用户自定义类型会带来两个问题：
1. 在"代码与数据统一在 KV 树"的前提下，自定义类型需要一个独立的类型注册表——这是一个平行于 kvspace 的状态，打破了"kvspace 是唯一事实源"的原则。
2. 自定义类型与 kvspace 的 kindexpr 系统会产生语义重叠，需要额外的转换层。
"## -> /lib/kvlang/规范/设计理由/无用户自定义类型
}
