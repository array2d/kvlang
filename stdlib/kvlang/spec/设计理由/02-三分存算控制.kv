// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/三分存算控制 {
	r##"# 为什么要三分：存储、计算、控制流严格独立

传统 VM 把存储（栈+堆+全局段）、计算（ALU 指令内联寻址）、控制流（整数 PC）混在同一内存空间里。这个设计对单机批量执行是高效的，但带来三个顽疾：

1. **崩溃不可恢复**：PC 是整数，崩溃后进程消失，执行状态无法独立持久化。
2. **调试需要附着进程**：状态在私有内存，必须 attach 调试器才能观察。
3. **算子与调度器深度耦合**：计算与控制流混在同一线程，难以独立替换或分布式拆分。

kvlang 的回答是：**把存储提升到 kvspace（独立于进程的 KV 树）**，计算（op-plat）与控制流（kvcpu）都成为无状态的执行者，只消费或导航存储层，自身不持有状态。

三分之后：崩溃恢复只需读回 PC 路径（已存于 KV）；调试只需 `kvspace tree`，无需 attach；算子可独立替换，调度层可分布式拆分。
"## -> /lib/kvlang/规范/设计理由/三分存算控制
}
