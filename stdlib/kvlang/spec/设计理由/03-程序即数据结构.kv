// 欢迎加入kvspace世界
lib kvlang/spec/设计理由/程序即数据结构 {
	r####"# 设计理由：程序即数据结构

## 从 Wirth 公式出发

Niklaus Wirth 的经典公式「程序 = 数据结构 + 算法」是正确的观察：程序的精华在于选对数据结构，算法随之
清晰。但这个公式隐含了一个假设——程序员自由定义数据结构。

kvlang 的主张是：**程序 = 数据结构 + 函数 + 数据**，且数据结构的组织机制全部内建（键族 + `struct` 原
型），不引入用户自定义的类型系统。

## 为什么不做 class/继承/typedef 那层类型系统

用户自定义类型（class + 继承、typedef 别名、独立类型注册表）的本质是：在语言的类型系统之上再建一层私
有命名空间，描述"
这些字节如何解释"。这在私有内存模型下有必要，因为内存中没有自描述能力。

kvlang 的存储层（kvspace）是自描述的：每个 XValue 携带 langtype，路径携带层次语义（`/`、`·`、坐标）。
在这个基础上，键族（`base·field`、`base·[i]`）已足以表达异构命名成员，故 kvlang **不做** class/继承/
typedef 这层类型系统，只保留 `struct` **原型**——原型本身也是 KV 树里的数据（`/lib/<Name>` 索引节点，
字段类型与默认值都是 KV 数据），不引入平行于 kvspace 的注册表（见 [[struct]]）。

强行引入平行类型注册表会带来两个问题：
1. 在"代码与数据统一在 KV 树"的前提下，注册表是平行于 kvspace 的状态，打破了"kvspace 是唯一事实源"的
   原则。
2. 它与 kvspace 的 langtype 系统语义重叠，需要额外的转换层。

## 键族仍是成员组织的第一机制

键族（key family，`base·field`、`base·[i]`）是 kvspace 路径天然支持的组合方式。把"数据结构"降格为"键的
命名约定"，好处是：

- **无平行类型注册**：键族的结构由 rwfunc 的读写模式与 `struct` 原型数据共同决定，不需要语言外的类型
  注册表。
- **跨节点天然共享**：任何连接到同一 kvspace 的节点都能按同样的命名约定访问同一份数据，无需序列化/反
  序列化。
- **调试透明**：`kvspace tree base·` 直接列出该数据结构的所有字段，无需类型系统辅助。

代价是：kvlang 无法在 layout 阶段对"结构完整性"做静态检查——rustc 能检查 struct 字段必须全部初始化，
kvlang 不能。这是已知取舍，kvlang 以 runtime 报错替代静态检查。

## 为什么同一语法同时承担三种职能

「VM 指令 / 高级语言 / 编译器 IR 三者合一」是 kvlang 设计哲学的集中体现，而非偶然。

传统上，高级语言源码经编译器降低为 IR，IR 再编译为机器码，三层各有独立表示，不可互换。kvlang 的扩展
编译器（作为 rwir 扩展）把 rwfunc 融合成单条 rwir——融合后的 rwir 仍然以同样的 kvspace 路径存在，可被
同样的 `kvspace tree` 观察，可被同样的 runtime 解释。没有独立的 IR 格式，也没有独立的二进制格式。

这个设计让"算子融合"成为 kvlang 成为训推引擎前端的关键机制，而不是一个需要独立工具链的编译器优化流水
线。
	"#### -> /lib/kvlang/spec/设计理由/程序即数据结构
}
