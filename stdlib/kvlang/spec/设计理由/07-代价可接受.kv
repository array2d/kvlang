// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/代价可接受 {
	r##"# 为什么代价可以接受

代价可以接受，因为 kvlang 的定位是**调度层**而非计算层。

最耗时的操作是 tensor 计算，这部分不在 kvlang 内执行——op-gpu 将 rwfunc 编译为 GPU kernel（TileLang AOT → `.so` → dlopen），由 GPU 硬件加速。kvlang runtime 只做调度：判断走哪条路径、调用哪个 rwir（经 def rwir 路由）、读写哪个 slot。调度层的绝对耗时在 tensor 计算面前可以忽略。

透明性在调度层兑现，算力在扩展引擎层找回——这是 kvlang 选择明文的根本底气。
"## -> /lib/kvlang/规范/设计理由/代价可接受
}
