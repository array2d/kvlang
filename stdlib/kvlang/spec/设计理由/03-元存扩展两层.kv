// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/元存扩展两层 {
	r##"# 为什么元存与扩展存储要再分两层

存储层内部还有一道分裂：元存（kvspace 核心，管理 slot/索引/指令树/帧栈）与扩展存储（tensor data，在 GPU 显存或本地 shm）。

这道分裂的动机是：

- **元存全局公有**：所有 worker 连同一 kvspace 即可共享代码（`/lib/`）与执行状态（`/vthread/`），不需要独立调度器元数据中心。这是分布式协作的基础。
- **扩展存储连接者私有**：GPU 显存本质上只有直连进程可访问，强行放入元存既无法实现又带来巨大序列化开销。tensor 数据由 heap-plat / op-plat 直接管理，跨卡搬运（AllReduce 等）在算子层显式处理。

铁律由此自然推出：kvspace 只存 tensor 元数据与扩展存储句柄，不存 tensor 原始数据。
"## -> /lib/kvlang/规范/设计理由/元存扩展两层
}
