// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/自洽取舍体系 {
	r##"# 语言是自洽的取舍体系，不是特性集合

最常见的语言设计误区是"堆砌特性"：看到其他语言有某个特性，就加进来。这会产生语义冲突、维护成本爆炸，最终让语言变成一个没有中心的杂货铺。

成熟语言的核心价值是**贯穿全栈的统一设计哲学**：类型系统、求值策略、内存模型、控制流模型、错误语义、可观测性模型——这些维度必须服从同一套顶层约束，彼此一致，而非各自为政。

kvlang 的统一约束是：**kvspace 是唯一状态持有者，代码与数据都在 KV 树里**。这个单一公理驱动了存算分离、明文化、无用户自定义类型、PC 是字符串等一系列决策——它们不是独立特性，是同一个公理在不同维度的投影。
"## -> /lib/kvlang/规范/设计理由/自洽取舍体系
}
