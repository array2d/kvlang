// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/三级层次 {
	r##"# 为什么需要三个层级

代码组织层级是语言的骨架。层级太少则无法表达大型程序的模块化需求；层级太多则概念负担重、规则相互干扰。

kvlang 选择三级：lib（包）、rwfunc（函数）、rwir（原子指令）。选三级的根本理由是：这三级与 kvspace 树的层次结构天然对齐，每级都有对应的路径层次，没有一级是多余的抽象。if/while/for 等控制流结构由 layout lower 阶段展平为 goto/br，不构成独立的代码组织层级。
"## -> /lib/kvlang/规范/设计理由/三级层次
}
