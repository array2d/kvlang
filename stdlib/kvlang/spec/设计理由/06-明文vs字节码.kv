// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/明文vs字节码 {
	r##"# 为什么选择明文而非字节码

传统 VM 以字节码为核心：PC 是整数偏移、opcode 是单字节枚举、类型在编译期丢失、运行时状态只有调试器能读。这是为解释执行速度而做的取舍——紧凑的二进制表示减少内存占用和取指开销。

kvlang 反向选择：**一切关键信息以明文字符串存于 kvspace 树**。动机是透明性：

- **对人透明**：`kvspace tree` 命令不需要反汇编器，直接读出代码、数据、执行状态的当前快照。
- **对 agent 透明**：agent 用同一套 `kv.Get` / `kv.Set` API 读写，程序自省与 agent 协作共享同一份数据，无需单独设计"可观测性接口"。

这个选择有直接代价：PC 是字符串而非整数、opcode 是 `"+"` 而非单字节——字符串比较和路径解析的开销显著大于整数操作。
"## -> /lib/kvlang/规范/设计理由/明文vs字节码
}
