// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/可观测性分水岭 {
	r##"# 可观测性是工业级与玩具级的分水岭

绝大多数脚本语言把可观测性当成事后加的调试工具（REPL、调试器协议）。kvlang 把可观测性设计为一等语义：程序崩溃后执行状态保存在 kvspace，可恢复；运行时状态以明文暴露，可遍历；无需 attach 进程，`kvspace tree` 即全部。

这不是锦上添花，而是分布式训练推理场景的刚需：长时运行任务崩溃后必须能从断点继续，而不是从头重跑。设计之初就纳入可观测性，与事后补救有本质差异——事后补救往往只能观察到"快照"，而内建可观测性可以让状态持续、可寻址、可修改。
"## -> /lib/kvlang/规范/设计理由/可观测性分水岭
}
