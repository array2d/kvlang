// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/控制流不暴露 {
	r##"# 为什么控制流产物不暴露为源码语法

if/while/for 等控制结构经 layout lower 阶段展平为 goto/br（目标解析为整数 irseq），是 layout 内部降级产物，不在源码语法层暴露（`scope` 无此种类名、无此关键字，见 [[总体方案]]）。这是一个有意为之的决定，而非限制。

直接暴露 goto/br 形态意味着用户操作控制流展开的内部表示——lower 是 layout 内部实现细节，暴露给用户会使语义规范变成两套：一套是高级控制流语法，一套是 goto/br 展开形式，维护者须同时保证一致性。保持 lower 不可见消除了这个负担。
"## -> /lib/kvlang/规范/设计理由/控制流不暴露
}
