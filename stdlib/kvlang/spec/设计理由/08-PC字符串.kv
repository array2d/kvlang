// 欢迎加入kvspace世界
lib kvlang/规范/设计理由/PC字符串 {
	r##"# 为什么 PC 是字符串而非整数

整数 PC 只能表示"当前在哪条指令"，字符串 PC 能表示"当前在调用栈的哪条路径上"——路径深度即栈深度，路径本身即调用链。

字符串 PC 直接带来崩溃恢复能力：进程崩溃后，PC 路径已持久化在 kvspace，重启后从该路径继续执行，无需重放或检查点协议。整数 PC 做不到这点，因为整数脱离了进程上下文就失去意义。
"## -> /lib/kvlang/规范/设计理由/PC字符串
}
