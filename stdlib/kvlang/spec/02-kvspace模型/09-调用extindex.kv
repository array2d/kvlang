// 欢迎加入kvspace世界
lib kvlang/spec/kvspace模型/调用extindex {
	r####"# 调用：extindex 复用指令树

调用一个函数时，运行时**不**拷贝指令，而是为本次调用建帧子树，令帧根成为指向 `/lib/{pkg}·{name}/` 指
令树的 extindex 叠加层，再把实参 / 结果地址写进帧根下的 `[0,±j]` 槽：

- `/lib/{pkg}·{name}.[0,±j]`（rwfunc 的**参数定义键**，点后缀、与坐标斜杠键 `/[0,±j]` 不同名）：
  langtype=`def langtype`，body=该参数名字 + NUL + 类型串。此键是**签名元数据**（dump / 类型检查
  用），**不参与运行时形参解析**；函数体指令里的形参引用已在 layout 编译期替换为帧坐标的显式解引用
  `*[0,-1]`。目录根**无** `/[0,±j]` 静态坐标槽——运行时向 `frameRoot/[0,±j]` 写实参地址 Ptr 时，
  该坐标在只读 extindex 目标（函数树）中并不存在，extindex 写保护正因**该名在扩展层缺席而放行**帧本
  地写，实参内容零拷贝。（反之若 layout 预落 `/[0,±j]` 静态槽，写保护会因扩展层已有同名只读节点而拒
  写、丢参——故 rwfunc 目录根不落这些坐标。）
- 所有帧共享 `/lib` 下同一份指令树，零拷贝；帧根下叠加各自的实参槽与局部变量。

崩溃恢复由此自然成立：PC 是路径字符串、帧根落 KV、返回点落 KV——进程重启后可从 kvspace 续跑。

## 参数解析：一层引用（显式解引用）

函数体指令里的形参引用，layout 编译期把参数名替换为**对帧槽坐标的显式解引用** `*[0,-1]`。帧槽
`frameRoot/[0,-1]` 由调用点写入实参地址 Ptr（ref=1，langtype=参数类型，body=实参地址），`*` 解引用
一次即得实参真实值：

```
1. frameRoot/[0,-1]   → 实参 key 路径      槽 → 实参地址（调用点写入的帧本地 Ptr）
2. *[0,-1]            → 值                 显式解引用一次
```

读写对称：读参 `*[0,-k]` 读到实参值；写参 `*[0,+k]` 指回调用者写槽，子帧写参直写调用方目标位置。读参
地址可指向调用方值以实现零拷贝重定向（见 [[系统变量]] 的 `‥rparam` / `‥wparam`）。形参类型随帧槽的
Ptr langtype 承载，不在解析链上额外查参数定义键。

写侧关键约束：kvspace 对 Set/Del **不穿透整键**（Ptr 键的写/删只作用该键本身，不追 target）——自动
穿透会把「写实参地址 Ptr 到帧槽」重定向到 target，导致丢参 / 写坏被指向的值。解引用一律由 runtime 显
式 `*` 掌控（见 [[ptr]]）。

## init 与标签

- lib 内的裸指令收集为隐式 `init` 函数（无 `init` 关键字），同样以上述布局落 `/lib/{pkg}·init/`。
- 具名块的标签落 `/lib/{pkg}·{name}/‥labels/{label}` → 指令序号（`layout/src/code.rs`、
  `layout/src/keytree.rs`）。

签名类型表达式文法见 [[文法]]；帧的系统变量见 [[系统变量]]；坐标键与 langtype 的分隔符语义见 [[键系
统与数组访问]]。
	"#### -> /lib/kvlang/spec/kvspace模型/调用extindex
}
