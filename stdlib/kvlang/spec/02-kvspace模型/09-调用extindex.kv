// 欢迎加入kvspace世界
lib kvlang/spec/kvspace模型/调用extindex {
	r####"# 调用：extindex 复用指令树

调用一个函数时，运行时**不**拷贝指令，而是为本次调用建帧子树，令帧根成为指向 `/lib/{pkg}·{name}/` 指
令树的 extindex 叠加层，再把实参 / 结果地址写进帧根下的 `[0,±j]` 槽：

- `/lib/{pkg}·{name}/` 下（rwfunc）只有命名参数键（`x` → body `"[0,-1]"`、langtype 为该参类型的
  Ptr），**无** `[0,±j]` 静态坐标槽。运行时向 `frameRoot/[0,±j]` 写实参地址 Ptr 时，该坐标在只读
  extindex 目标（函数树）中并不存在——extindex 写保护正因**该名在扩展层缺席而放行**帧本地写，实参内容
  零拷贝。（反之若 layout 预落 `[0,±j]` 静态槽，写保护会因扩展层已有同名只读节点而拒写、丢参——故
  rwfunc 目录根不落这些坐标。）
- 所有帧共享 `/lib` 下同一份指令树，零拷贝；帧根下叠加各自的实参槽与局部变量。

崩溃恢复由此自然成立：PC 是路径字符串、帧根落 KV、返回点落 KV——进程重启后可从 kvspace 续跑。

## 参数解析：指针链

指令读到 `rwir:x` 时，`x` 是相对当前帧的名字。运行时经指针链解析到实参真实位置：

```
1. 读 frameRoot/x        → "[0,-1]"          名字 → 槽坐标
2. 读 frameRoot/[0,-1]   → 实参 key 路径      槽 → 实参地址
3. 读该路径              → 值                 解引用
```

第 1 跳的 `frameRoot/x` 由 `/lib` 指令树经 extindex 透出（layout 写入、只读；其 Ptr 的 langtype 记参
数类型，运行期解析只取 body 坐标），第 2 跳的 `frameRoot/[0,-1]` 由运行时在调用点写入。读参地址可指
向
调用方值以实现零拷贝重定向（见 [[系统变量]] 的 `‥rparam` / `‥wparam`）。

## init 与标签

- lib 内的裸指令收集为隐式 `init` 函数（无 `init` 关键字），同样以上述布局落 `/lib/{pkg}·init/`。
- 具名块的标签落 `/lib/{pkg}·{name}/‥labels/{label}` → 指令序号（`layout/src/code.rs`、
  `layout/src/keytree.rs`）。

签名类型表达式文法见 [[文法]]；帧的系统变量见 [[系统变量]]；坐标键与 langtype 的分隔符语义见 [[键系
统与数组访问]]。
	"#### -> /lib/kvlang/spec/kvspace模型/调用extindex
}
