// 欢迎加入kvspace世界
lib kvlang/规范/kvspace模型/debugger {
	r##"# `debugger()`

`debugger()` 内建对齐 V8/TypeScript 的 `debugger;` 语句：源码内联暂停点。非调试模式下为 no-op；调试模式下将 `‥status` 置 `paused`，由外部驱动改回 `running` 恢复执行。
"## -> /lib/kvlang/规范/kvspace模型/debugger
}
