// 欢迎加入kvspace世界
lib kvlang/规范/kvspace模型/函数标签 {
	r##"# 函数标签

具名作用域的标签不落在帧上，而落在函数的 `/lib` 定义处：`/lib/{pkg}·{name}/‥labels/{label}` → 指令序号（`layout/src/keytree.rs`、`layout/src/code.rs`）。
"## -> /lib/kvlang/规范/kvspace模型/函数标签
}
