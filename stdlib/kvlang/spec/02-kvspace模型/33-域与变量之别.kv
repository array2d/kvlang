// 欢迎加入kvspace世界
lib kvlang/规范/kvspace模型/域与变量之别 {
	r##"# 域与变量之别

注意区分：[[地址空间]] 的结构**域**（`/lib`、`/vthread`、`/networld` 顶层树）与对象随身的 `‥` 系统**变量**是两种机制。域是路径前缀赋予的固定结构语义，系统变量是任一对象 `/` 子树内的影子元数据。
"## -> /lib/kvlang/规范/kvspace模型/域与变量之别
}
