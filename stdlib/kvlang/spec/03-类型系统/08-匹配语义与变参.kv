// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/匹配语义与变参 {
	r####"# 匹配语义与变参

本章匹配语义作用于 **def langtype**（定义处的匹配类型）：把一个值的 **langtype** `(kind,ndim,dims)` 与签名
里的 def langtype 判定是否命中（两者区别见 [[langtype签名类型表达式]]）。匹配**恒逐值、无状态**：只看单个
值,不在参数间携带任何环境（跨操作数的维相等/输出推断不在类型层,归 op 的 shape function,见
[[runtimec内建]]）。

## 匹配语义

一个值（种类 `k`、维数 `n`、维列表 `d`）匹配类型表达式 `E`：按 `|` 拆成若干 atom，**任一** atom 命中
即匹配。单个 atom 的判定：

1. atom 为 `any` → 命中。
2. atom 含 `·`（mapexpr）→ 命中当且仅当 `k == stringkeymap`。键/值结构在**匹配期不再校验**（已在装载
   期校验合法性）。
3. atom 有 `[` 前缀（shape）→ 先按维度段做形状匹配，再以剩余基种类匹配 `k`。
4. atom 无 `[` 前缀（标量）→ 命中当且仅当 `n == 0` 且基种类等于 `k`（`any` 除外走精确字符串相等）。

**形状匹配**（维度段的轴量词序列 → `n`,`d`，轴量词见 [[文法与合法性]]）。一个 dims 至多一个变元量词
（`?`/`*`/`+`），故切分唯一、无回溯：设定长元（整数或 `.`）共 `f` 个，令 `rem = n - f`：

| 维度段 | 命中条件 |
|--------|---------|
| 无变元（全定长元） | `n == f`；逐轴：整数元须 `d[i] == 该整数`，`.` 任意 |
| 含一个 `?` | `rem ∈ {0,1}`；`?` 吸收 `rem` 个轴，其余定长元按位（`?` 前后各就各位）逐轴判定 |
| 含一个 `*` | `rem ≥ 0`；`*` 吸收 `rem` 个轴，其余定长元按位逐轴判定 |
| 含一个 `+` | `rem ≥ 1`；`+` 吸收 `rem` 个轴，其余定长元按位逐轴判定 |

空 `[]` **等价** `[.]`——「恰一维、任意长」。字符串标注 `[]char/<编码>` 即一维字符序列。例：`[.]`↔恰
1 维;`[.,.]`↔恰 2 维;`[*]`↔任意秩;`[*,.]`↔秩≥1、末维任意;`[512,*]`↔首维 512、秩≥1。

任一失配即 TypeError：装载期由 layout 报诊断，运行期写 vthread 错误并停机。

## 参数变参（签名层,非类型）

一个**参数位**吸收 0..N 个同型实参（`print`/`println`/`min`/`max` 等 arity 开放算子）是**签名**的
arity 属性,**不属于类型表达式**——源码在末读参尾缀 `...` 声明,由签名解析器剥离,落成签名行锚点 `[0,0]`
的 `dynamic` 字节,该参数 langtype 子槽保持纯净（见 [[函数]]、[[指令布局格式]]）。派发时:`dynamic=1` →
末读参对**每个**尾随实参按其（纯净）langtype 逐个判定。`LangtypeValid`/`LangtypeMatch` 永不见 `...`。

XValue head 三正交维编码见 [[总体方案]]（本卷只界定 langtype 串的文法与匹配语义）。

## 多态派发

- **native 算子**（如 `+`）内建多态，签名只做类型检查；分派在 C 算子内部按实参种类完成。
- **rwir 指令**（如 `numpy·add`）经 def rwir 路由到有对应 myrwircaps 的 runtime，按实参实际种类分
  派；高维形状由该 runtime 自行处理。
- **用户函数**（rwfunc）以并集声明接受集，函数体内用种类分支处理各分支。

## 语法冲突规避

签名以 `,` 分隔参数，形状维度段内也用 `,` 分隔维。签名解析器**跟踪方括号深度**：见 `[` 进形状态、见
`]` 退出；形状态内的 `,` 归维、态外的 `,` 归参数。类型表达式内不出现 `(` `)` `:` `->` 空格，故与签名
外层结构无歧义。
"#### -> /lib/kvlang/spec/类型系统/匹配语义与变参
}
