// 欢迎加入kvspace世界
lib kvlang/规范/类型系统/变参 {
	r##"# 变参 `...`

变参尾缀 `...` **仅**允许加在**末位读参**的类型表达式上（如 `A:any...`、`A:int64|float64...`），表「0..N 个同型实参」，用于 `print`/`println`/`min`/`max` 等 arity 开放的算子。

- 非末位参数带 `...`：装载期错误。
- 写参（返回值）带 `...`：装载期错误。

匹配时，变参把所有尾随实参逐个按去掉 `...` 后的类型表达式判定。
"## -> /lib/kvlang/规范/类型系统/变参
}
