// 欢迎加入kvspace世界
lib kvlang/规范/类型系统/两形态互转 {
	r##"# 两形态互转

| 算子 | 语义 |
|------|------|
| `array·scatter(a)` | compact → stringkeymap（散 key） |
| `array·compact(b)` | stringkeymap → compact |

字面量初始化默认按括号定形态；`scatter`/`compact` 显式转换（见 `tutorial/04-ndarray/separated.kv`）。
"## -> /lib/kvlang/规范/类型系统/两形态互转
}
