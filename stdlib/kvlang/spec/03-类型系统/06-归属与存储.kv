// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/归属与存储 {
	r####"# 归属与存储

| 归属 | 存放位置 | 种类 |
|------|---------|------|
| 用户函数签名 | `/lib/<pkg>·<name>` | `rwfunc` |
| def rwir 路由头 | `/lib/<opcode>` | `def rwir` |
| native 算子签名 | C 注册表（不落入 kvspace） | —— |

**铁律**：`rwfunc` 每个参数落入 kvspace 的 **def langtype**（参数定义键 body 的类型串，即定义处的匹配类
型）与源码里的类型标注**逐字节相同**（同一文法；变参尾缀 `...` 除外——它是签名层 arity、解析时剥离，见
[[函数]]）；`def rwir` 源码不声明，其参数 def langtype 由各 runtime 注册时按同一文法写入。签名**不写
进**计数头节点的 langtype、也不塞进其 body，分两处存：

1. **计数头**：`rwfunc` 落 `/lib/pkg·funca/[0,0]`（langtype `rwfunc`）、`def rwir` 落 `/lib/<op>` 节点自
   身（langtype `def rwir`），body 只记 `[nr:u16 LE][nw:u16 LE][dynamic:u8]`——读参个数 `nr`、写参个数
   `nw`、是否变参 `dynamic`（末位读参 `...`）；**不含任何参数 langtype 串**。
2. **参数类型承载**（按符号分两路，见 [[storetype_def族与签名行]]）：
   - `def rwir` → **位置化签名行 `[0,x]`**（`x<0` 读参、`x>0` 写参、`[0,0]` 锚点），每槽 `def langtype`，
     body = 该参数完整 langtype 串（跨 runtime 队列按位置收参，故须有序）。
   - `rwfunc` → **参数定义键 `funcDir.[0,±j]`**（点后缀，与坐标斜杠键 `/[0,±j]` 不同名），每键
     `def langtype`，body = 参数名 + NUL + 类型串；函数体形参引用改走帧坐标，运行期不查此键（见
     [[调用extindex]]）。

故计数头节点的 langtype 就是裸种类名 `rwfunc`（rwfunc 的 `[0,0]`）/`def rwir`（def rwir 的
`/lib/<op>` 节点自身）本身，不再是「签名 langtype 串」。native 算子的 langtype 内联在 C 注册表中，是唯
一权威来源，运行时匹配**直接读 C 表、不回查 kvspace**；数值多类型算子在 C 表中融合为单条（如
`add(a:int8|…|float64, b:…)`）。

## 强制类型标注

`rwfunc` 签名中每个参数与每个返回值**必须**声明类型（`name:type_expr`）。缺标注的签名**拒绝装载
**——layout 在语法检查期报错，指出该参数/返回值无类型标注。类型标注须通过 langtype 合法性校验（拒绝非
种类名，见下）。`def rwir` 路由头由 runtime 注册（源码不声明），其参数类型在注册接口处按同一合法性规
则校验。
"#### -> /lib/kvlang/spec/类型系统/归属与存储
}
