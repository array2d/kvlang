// 欢迎加入kvspace世界
lib kvlang/规范/类型系统/强制类型标注 {
	r##"# 强制类型标注

`rwfunc` 签名中每个参数与每个返回值**必须**声明类型（`name:type_expr`）。缺标注的签名**拒绝装载**——layout 在语法检查期报错，指出该参数/返回值无类型标注。类型标注须通过 kindexpr 合法性校验（拒绝非种类名，见下）。`def rwir` 路由头由 runtime 注册（源码不声明），其参数类型在注册接口处按同一合法性规则校验。
"## -> /lib/kvlang/规范/类型系统/强制类型标注
}
