// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/storetype_def族与签名行 {
	r####"# storetype——签名行、def 族与目录节点

### 签名不入 langtype 串，而入签名行 `[0,x]` 槽

> **铁律**：任何 rwir/rwfunc 值的 body = `[nr][nw][dynamic]` 计数，仅此而已。参数类型的承载则按种类
  分
  两路：**def rwir** 每参独占一个 `[0,x]` 参数轴坐标 key（位置化）；**rwfunc** 不落 `[0,x]`，改由命
  名
  参数键 `{name}` 的 Ptr（其 langtype 即该参类型）承载（按名解析）。

rwfunc / def rwir 的**签名（各参数类型）不写进 langtype 串**，也不塞进主槽 body。主槽只记计数头，参
数
类型按种类分路承载：

1. **主槽计数头**（两者共有）：目录节点主槽 `{fn}`（如 `/lib/pkg·funca`）的 body 只记
   `[nr:u16 LE][nw:u16 LE][dynamic:u8]`——读参个数 `nr`、写参个数 `nw`、是否变参 `dynamic`（末位读参
   `...`）。**不含任何参数 langtype 串**。
2. **def rwir——位置化 `[0,x]` 签名槽**：每个参数类型落该目录签名行坐标 `[0,x]`——`x<0` 读参、`x>0`
   写参、`[0,0]` 为锚点（见 [[指令布局格式]] 二元坐标 `[s0,s1]`，`s0=0` 即签名行）。每个 `[0,x]` 是
   **`def langtype`** xvalue，body 存该参数完整 langtype 串（一参一槽），**纯净、不含变参 `...`**——
   变
   参是 arity，只落主槽 `dynamic` 字节。跨 runtime 队列按位置收参，故须有序签名。
3. **rwfunc——命名参数键 `{name}` 承载**：func 目录根**不**落 `[0,x]` 槽；每个参数类型随命名参数键
   `funcDir/{name}` 的 Ptr 承载——该 Ptr body 为槽坐标（读参 `[0,-j]`、写参 `[0,+j]`），**其 langtype
   即该参数完整类型串**。rwfunc 按名在帧内解析，不在意参数顺序、仅区分读/写参。

于是 rwfunc/def rwir 主槽 langtype 都是裸种类名 `rwfunc` / `def rwir` 本身，**非「签名 langtype
串」**。def rwir 的签名散在 `[0,x]` 槽、rwfunc 的散在 `{name}` Ptr；实现指令行（若有）在
`[s0>0,·]`。

### def 族：定义节点的 langtype（带空格书写）

「某类型的定义节点」自身的 langtype 用 **`def <base>`** 家族名，**中间保留空格**（读作「`base` 的定
义」）：

| langtype | 落点 | 表示 |
|----------|------|------|
| `def rwir` | `/lib/<op>` | 一条 rwir 的路由头/签名定义（原 `defrwir`，废连写） |
| `def struct` | `/lib/<Name>` | 一个 struct 原型的定义节点（字段类型定义在其子槽） |
| `def langtype` | def rwir 签名行 `[0,x]` 槽 | 一个参数的类型定义（body=该参数 langtype 串；rwfunc 不用此槽，类型随 `{name}` Ptr 的 langtype 承载） |

`def` 与 base 间的空格是**规范强制**：连写形式 `defrwir`/`defstruct`/`deflangtype` 一律废除。

### 目录节点即索引型容器

KV 树中的目录路径（尾 `/`）本身是 xvalue，storetype ∈ {index, extindex}，langtype 表达目录语义：

| 路径 | langtype | storetype | 成员 |
|------|----------|-----------|------|
| `/lib/` | `lib` | index | `/lib/<pkg>` 各包目录（`/` 子路径） |
| `/lib/pkg·funca/` | `rwfunc` | index（静态函数定义目录） | 命名参数键 `{name}`（Ptr，langtype=参类型）+ 实现指令行 `[s0>0,·]` |
| `/lib/<op>` | `def rwir` | index（路由头，只有签名行） | 签名行 `[0,x]`（def langtype），无实现指令行 |
| `/vthread/1/` | `rwfunc` | extindex（运行栈，可扩展） | `/vthread/1/<坐标>` 栈帧/指令 |

同为 rwfunc，静态定义落 `index`、运行栈落 `extindex`——langtype 一致、物理载体按场景选。
"#### -> /lib/kvlang/spec/类型系统/storetype_def族与签名行
}
