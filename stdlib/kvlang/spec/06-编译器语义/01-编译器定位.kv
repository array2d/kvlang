// 欢迎加入kvspace世界
lib kvlang/spec/编译器语义/编译器定位 {
	r####"# 编译器定位

kvlang 的三阶段：**layout（o0）→ 编译器（优化）→ runtime（纯解释）**。

- **layout** 是 o0 编译器：只做检查、布局、降级，**不做任何优化**（见 [[范围与一致性]]、卷 04）。一旦在 layout 就优化即丢失高层语义，使编译器无法按原语义在异构硬件上重新优化。
- **runtime** 纯解释：**不编译、不优化、不重排**，只按 PC 逐条取指解码执行（见 [[执行模型]]）。
- 二者之间的**优化**留给「扩展编译器」——本卷即界定它。卷 00 已用「扩展编译器」一词指代按原语义在异构硬件上做优化的实体，本卷给出其契约。

## 编译器不属语言核心

编译器是**扩展**，不是 C runtime 核心的一部分。它是计算引擎（deepx-cpu-compute、deepx-gpu-compute 等算子融合计算引擎）的组成，与扩展运行时**同架构**：嵌入 C runtime 核心，在其上追加算子表示与编译。本规范只界定**编译器与核心之间的契约**——编译器读什么、往 `/lib` 写什么、runtime 据此如何派发——**不**规定编译器内部算法（选择策略、融合规则、代价模型由各计算引擎文档界定）。

## 与 layout 的根本区别

| | layout（o0） | 编译器 |
|---|---|---|
| 读 `/lib` | 否——只写自身产物 | **是——读整棵 `/lib`**，检查当前注册的所有后端算子实现 |
| 视野 | 单函数/单次布局，静态 | 全局：已注册后端 + 全部调用点 |
| 产物 | 首次布进 `/lib` 的 rwfunc/指令 | 就地改写调用点 opcode、新增版本化函数，仍是合法 `/lib` 树 |
| 是否可选 | 必经 | 可选——未经编译的 `/lib` 树 runtime 亦能跑（除抽象算子外，见 [[前端算子与后端算子]]） |

编译器的输入是**一棵已 layout、且各计算 runtime 已注册其 myrwircaps（后端算子 def rwir 路由头，见 [[rwfunc布局与def_rwir]]）的 kvspace `/lib` 树**；输出仍是合法 `/lib` 树，交 runtime 纯解释。

## 三职责

1. **后端绑定**——把前端抽象算子调用点（`deepx·argmax`）改写为具体后端算子（`deepx/miaobyte·argmax`），见 [[前端算子与后端算子]]。
2. **复合算子分解/融合**——把由基础算子组合的复合 rwfunc（如 `softmax` = exp/sum/div）融合成单条自解释 rwir。
3. **算子版本化**——融合产物作为版本递增并存，runtime 调用时优先高版本，见 [[算子版本化与高版本优先]]。

## 融合的层级：编排器 vs kernel 编译器

融合不是一步，分**两层**，职责必须切开：

- **kvlang 编译器（编排器）**：读整棵 `/lib`，做后端绑定、复合算子分解、**识别可融合的相邻 rwir 组**、编排、并把结果**回写**——把融合产物注册为 `/lib/<opcode>` def rwir + myrwircaps、改写调用点 opcode、登记版本。回写是编排器的职责。
- **kernel 编译器（叶子 codegen）**：把「一组 rwir + shape + dtype」codegen 成**单个硬件 kernel**（如 op-gpu / TileLang，GPU 上多个 `deepx·{rwir/rwfunc}` 融成一次 kernel 调用 = 一条 rwir）。它是**纯函数式服务**：吃 `op序列 + shape + dtype`，吐 `.so + meta`，**不回写 kvspace、不感知调度/分布式**，便于独立测试与对拍。

对照 PyTorch：编排器 ≈ Inductor 的 lowering + Scheduler（识别融合组）；kernel 编译器 ≈ Inductor 的 codegen 后端（Triton / C++）。

**融合匹配须是子序列/滑窗匹配**（在 rwir 线性序列上），不可退化为整段 opcode 元组精确匹配（否则每种顺序/shape 变体都要新增一条规则、不可扩展）。切分边界的硬启发式：**重算子（GEMM/conv 一类）计数**——一个融合组内重算子超阈值（典型 ≥2 GEMM）即拆成多 kernel。融合规则宜用「规则即数据」的表 + 四态标签（可融单核 / 缺模板 / 须拆多核 / 不可融）。

**GPU 融合产物走 AOT→`.so`→`dlopen`**（调用约定 `fn(ptr..., stream)`），运行时侧零 Python 依赖——这条约束决定必须 AOT 而非进程内 JIT，须作为独立可验收里程碑，不可停在 JIT 原型。

## 非规范对照（PyTorch）

本编译器的「编译期读 `/lib` 静态绑定后端」≈ TorchInductor 的 lowering（编译期查表把抽象 ATen op 静态映射到具体 kernel），**不是** `c10::Dispatcher` 的运行期按 device 位图派发。后端绑定表 ≈ Inductor lowering 表；复合算子分解 ≈ `torch/_decomp`；融合 ≈ Inductor Scheduler 合并算子成单 kernel；版本择优 ≈ autotune 缓存最优（但 kvlang 把「谁更优」显式化为可声明、可审计的静态版本号）。
	"#### -> /lib/kvlang/spec/编译器语义/编译器定位
}
