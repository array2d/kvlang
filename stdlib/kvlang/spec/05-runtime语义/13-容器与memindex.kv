// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/容器与memindex {
	r##"# 容器 = 值 + memindex

复合命名数据（`stringkeymap`、`struct`）在 kvspace 中的运行期表示为**值 + memindex**：`base` 键持容器值（含 dims、容量等头信息），成员 `base·name` 作为**独立子 key** 落库。`·`（memindex 标记）分出容器的成员空间；memindex 的成员坐标名（`stringkeymap` → stringkeymap index、`struct` → struct index）是成员集合的唯一权威，遍历成员**必须**据此枚举，**不得**假设成员键连续或有序之外的布局。

- **写成员**：`rec·name = 值` 的写槽是完整成员键 `rec·name`，`=` 为值拷贝。
- **读成员**：`rec·name -> v` 直接取 key `rec·name`；缺失成员的动态读（`kv·get`）返回 `None`。
"## -> /lib/kvlang/规范/runtime语义/容器与memindex
}
