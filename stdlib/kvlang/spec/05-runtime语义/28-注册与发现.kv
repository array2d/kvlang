// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/注册与发现 {
	r##"# 注册与发现

### 签名注册

扩展把每个 opcode 的签名写到 `/lib/<opcode>`（kind=`def rwir`），格式为：

- 主槽 body：`[nr:u16 LE][nw:u16 LE][dynamic:u8]`——读参个数 `nr`、写参个数 `nw`、是否变参 `dynamic`。
- 签名行 `[0,x]` 各槽：每个参数类型落一个 `def kindexpr` xvalue（`x<0` 读参、`x>0` 写参、`[0,0]` 为锚点）。

例：`/lib/json·to`（nr=1, nw=1）→ 主槽 body=[1,1,0]，`[0,-1]=def kindexpr("char/utf8")`，`[0,1]=def kindexpr("[]char/utf8")`。

- native builtin 的签名**不落盘**——在 C 注册表直接查表（见 [[函数调用与内建]]）。
- 扩展签名**落 `/lib/<opcode>`**，中央 runtime 经 `isothersrwir` 识别：`opcode[0]=='/'` → false；否则读 `/lib/<opcode>` 的 kind，等于 `def rwir` 即须经 def rwir 路由的 rwir。

### 全局标记与幂等

- 裸 opcode（如 `print`/`input`）须在**进程启动期**经 `kvlang_rwirextRegister` 注册全局，使 layout 阶段不给它补 `pkg·` 前缀；带 `·` 的限定 opcode（`json·to`）本就不补前缀。
- 注册**必须幂等**：`vids` 队列已存在则跳过。否则脏 kvspace 上重复注册时，Set 经路径穿透会把首队列改成自指 `Ptr`，令 `resolve_path` 死循环（父子 kvlang 共享同一 redis 的挂起根因）。
"## -> /lib/kvlang/规范/runtime语义/注册与发现
}
