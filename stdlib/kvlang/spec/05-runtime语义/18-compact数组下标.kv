// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/compact数组下标 {
	r##"# compact 数组下标

数组有两种存储形态：**compact**（`[...]` 字面量，元素连续打包进单个 XValue，head kind = 元素类型）与 **stringkeymap**（`{...}` 字面量，散 key，每元素落 `base·i` 独立子 key）。下标 `a[i]` / `xv·at` / `xv·set` 用于 **compact** 形态；stringkeymap 形态用 `for-in` / `kv·get` 遍历访问。

- **下标读**：`a[i] -> e`（layout 脱糖为 `xv·at`/`kv·get`），按 `kvlangXvalueElemSize(kind)` 定的元素字节宽零拷贝定位第 `i` 个元素。
- **下标写**：`a[i] = v`（脱糖为 `xv·set`），就地改第 `i` 个元素。写下标**必须**用赋值式 `a[i] = v`；`->` 形式的写槽不得带下标（`v -> a[i]` 由 layout 拒绝）。
- `xv·at(a, i) -> e` / `xv·set(a, i, v) -> a` 是显式等价形式。

元素字节宽由 kind 决定（`char/utf8`=1，`int32`/`float32`=4，`int64`/`float64`=8……），数组与字符串的元素访问据此零拷贝定位。锚例：`tutorial/04-ndarray/`（`continuous.kv`、`subscript_write.kv`、`separated.kv`、`string_array.kv`）、`tutorial/05-dict/`、`tutorial/12-struct/`。
"## -> /lib/kvlang/规范/runtime语义/compact数组下标
}
