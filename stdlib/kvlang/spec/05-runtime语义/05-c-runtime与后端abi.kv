// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/cruntime与后端abi {
	r##"# C runtime 与后端 ABI

本章界定 runtime 的实现契约：统一的 `kvspace*` C ABI、C runtime 与 Rust runtime 的一致性要求、durable 单句柄相干、后端装载。

## 一、小 runtime 大扩展

中央 runtime 只实现执行核心（取指-解码-执行、call/return/br/goto、native builtin 派发、`=` 拷贝、用户函数调用）；tensor / LLM / agent / print / fs / http 全部交给扩展运行时（见 [[rwirext扩展]]）。这条分工是**不可越界的职责线**：runtime-c 独占三件事，扩展一件都不许重做。

**runtime-c 拥有（唯一实现，扩展不得复刻）**：

1. **执行核心**：取指-解码-执行大循环、call/return/br/goto、vthread 调度、native builtin 派发、`=` 拷贝语义。
2. **值语义与编解码**：TLV / kindexpr 的**唯一编解码器**在 kvspace 正典 codec（`kvspaceTlvEncode` / `kvspaceDecodeHead` / `kvspaceNewPtr`/`NewChar`/`NewBool`/`NewInt64`/`NewFloat64`）；runtime-c 的 `xvalue.c` 只是其薄封装。kindexpr 串是类型的唯一真相，怎么拼、怎么解，只此一处。
3. **指令解码与签名校验**：`rwir.c:RwirDecode`（稠密探查、遇空即停）、`/lib/<op>` 签名的读写码规范、`KindexprValid`/`KindexprMatch` 类型判定。

## 二、C runtime 结构

`kvlang/runtime/` 是通用嵌入式执行器（纯 C + `extern "C"`），可嵌入 rwirext 项目（op-gpu、agent、livebyte）。两组对外 C ABI：

- `kvlang_runtime.h`：核心执行器（`kvlangRuntimeConnect`/`Disconnect`/`Bootstrap`/`ExecuteVthread`/`KvspaceHandle` …）。
- `kvlang_rwirext.h`：扩展 runtime 语义，**只暴露 kvspace 不提供的部分**。现有 9 个符号：`Register`、`Handoff`、`NextPc`、`Params`、`ResolveRead`、`ResolveReadPath`、`ResolveWrite`、`KindexprValid`、`KindexprMatch`。KV 存取（connect/get/write/del/list/tlv）**不在此**——扩展宿主自连 kvspace ABI，把句柄（`void *`）传进带句柄的 rwirext 函数。

内部模块：`xvalue.c`（TLV 编解码薄封装）、`kindexpr.c`、`kv.c`（kvspace ABI 封装）、`keytree.c`（路径构造）、`rwir.c`（PC 解析 + Decode）、`vthread.c`、`builtin*.c`（native 算子 + 静态注册表）、`kvcpu.c`（Execute 循环）、`runtime.c`（ABI 层）、`rwirext.c`（扩展 ABI，含 `isothersrwir`）。native 注册表为**静态数组 + 线性查找**。

## 三、统一 `kvspace*` C ABI（去特化）

layout 与 runtime **都不对 kvspace 做特化**：不 dlopen、不双后端分支、不解析 scheme。二者只走一套 `kvspace*` 兼容 C ABI（无下划线 camelCase）：

```
kvspaceConnect / Close / Clear
kvspaceGet                       // 借用读：*out 指向后端常驻/回收空间，调用方不得 free
kvspaceWriteInPlace              // key 已存在且 body_len 相等 → 返回原 box body 偏移指针
kvspaceWriteNewPlace             // 否则按 (kindexpr, body_len) 分配新 box、写 head，返回 body 指针
kvspaceListLen / ListAt / Del / DelTree / Cp / CpTree
kvspaceMkindex / MkindexExt / RmindexExt / Watch
kvspaceTlvEncode / TlvEncodeMode / DecodeHead
kvspaceNewPtr / NewChar / NewBool / NewInt64 / NewFloat64
```

**零拷贝写读模型**（normative）：

- **读**走 `kvspaceGet`——返回后端常驻内存的**借用偏移指针**，调用方只读、**不得** `free`。
- **写**不经「先 encode 整条 TLV 再交后端」：向后端要一个可写 body 偏移指针再直接写字节——`kvspaceWriteInPlace`（key 已存在且 body 尺寸不变，原 box 就地改）与 `kvspaceWriteNewPlace`（新建 / 换 kind / 换尺寸，分配新 box 并写 head）两条原语，各自唯一、无中转 buffer。
- **前缀枚举**不一次性返回整段名单缓冲：`kvspaceListLen` 只返子项计数，`kvspaceListAt(idx)` 借用回收缓冲逐项取名——遍历即「先 ListLen 定计数、再逐 idx ListAt」。

**后端由链接决定**：构建期经环境变量选择链接哪个 kvspace 库（`kvspace-c` 或 `kvspace_durable`），两者导出同一 ABI；runtime 的句柄结构只保留 `void *h`，代码零后端分支。

## 四、三个后端

| 后端 | 库 | 存储 | 场景 |
|------|-----|------|------|
| shm | kvspace-c（C） | ART 树 + slotsboxmalloc + file-backed mmap | 高性能（训练/推理） |
| redis | kvspace-durable（Rust） | Redis | 基线验证 |
| fs | kvspace-durable（Rust） | 文件系统 | 本地持久化 |

`conn(dsn)` 按 scheme（`shm://` / `redis://` / `fs://`）选后端，DSN 是唯一的后端区分。三后端**语义必须完全一致**：一致性判据是 `tutorial/` 全量三后端逐字节相同输出（见导言卷）。后端存储布局差异（如 fs 把 `·` 编码为 `·/` 嵌套目录）经对称编解码还原，**不是**语义差异。

## 五、C runtime 与 Rust runtime 一致性

两套 runtime 必须表现相同动态语义，但**执行位置策略**允许不同：

- **C runtime**：扩展 rwir 一律经 `handoff_external_rwir`（Set `/lib/<op>/vids/<vtid>` + Watch 30s 超时）移交，供独立进程扩展消费；嵌入式模式（`KVMODE_RETURN`）经 `out_pc` 把控制交回同进程扩展。
- **Rust runtime**：`is_inproc(op)` 命中的 rwir 在驱动循环里就地 `dispatch`（无 handoff、无 watch）；非就地 rwir 经 `kvlang_rwirextHandoff` 移交独立进程扩展或委托宿主 `ext` 处理器。

二者对 native builtin、控制流、`=` 拷贝、值语义的处理**必须**一致；差异仅限扩展 rwir 的**就地 vs 移交**这一执行位置维度，且对 kvlang 程序不可观测（同一 tutorial 三后端两 runtime 输出一致）。

## 六、durable 单句柄相干

durable 后端（fs/redis）采用**惰性 flush**，只在**同一 kvspace 句柄内相干**。因此 C runtime 与其驱动的扩展（Rust `run_vid` 循环、层内 rwir）**必须共用同一 kvspace 句柄**（`kvlangRuntimeKvspaceHandle` 取出后传给 rwirext）。若各持独立句柄，一方的写对另一方不可见，fs/redis 的 PC / 值会读到陈旧快照，导致整段重跑或错乱。此为 normative 要求。

## 七、扩展宿主自连 kvspace

扩展宿主（op-gpu、numpy、Go json）**自己** `kvspaceConnect(dsn)` 拿句柄，rwir 移交只用 `kvlang_rwirext*` 的 runtime 语义（`Register`/`Params`/`ResolveRead`/`ResolveReadPath`/`ResolveWrite`/`NextPc`）。tensor 类大 buffer **应**走 shm 后端零拷贝：`ResolveReadPath` 拿容器路径 → `kvspaceGet` 取 TLV 指针 → `DecodeHead` 定位 body → 在该地址原位建 ndarray，读写 ndarray 即读写 kvspace，无拷贝；写走 `kvspaceTlvEncode` + `WriteInPlace`/`WriteNewPlace`。锚例：`tutorial/14-numpy/`（shm 全流程）、`tutorial/14-networld/`（fs/proc）。
"## -> /lib/kvlang/规范/runtime语义/cruntime与后端abi
}
