// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/动态成员名 {
	r##"# 动态成员名 `·*`

`base·*k` 用 `k` 的**值**作为成员名：读 `m·*k` 即以局部变量 `k` 当前的字符串值定位 `m·<k值>`。`·*` 用于局部容器变量的动态键访问；等价的显式形式为 `kv·get(m, k)` / `kv·set(m, k, v)`。
"## -> /lib/kvlang/规范/runtime语义/动态成员名
}
