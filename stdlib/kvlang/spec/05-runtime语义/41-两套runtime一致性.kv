// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/两套runtime一致性 {
	r##"# C runtime 与 Rust runtime 一致性

两套 runtime 必须表现相同动态语义，但**执行位置策略**允许不同：

- **C runtime**：须经 def rwir 路由的 rwir（不在本 runtime myrwircaps 内的 rwir）一律经 `handoff_external_rwir`（Set `/lib/<op>/vids/<vtid>` + Watch 30s 超时）移交，供独立进程扩展消费；嵌入式模式（`KVMODE_RETURN`）经 `out_pc` 把控制交回同进程扩展。
- **Rust runtime**：`is_inproc(op)` 命中的 rwir 在驱动循环里就地 `dispatch`（无 handoff、无 watch）；非就地 rwir 经 `kvlang_rwirextHandoff` 移交独立进程扩展或委托宿主 `ext` 处理器。

二者对 native builtin、控制流、`=` 拷贝、值语义的处理**必须**一致；差异仅限须经 def rwir 路由的 rwir 的**就地 vs 移交**这一执行位置维度，且对 kvlang 程序不可观测（同一 tutorial 三后端两 runtime 输出一致）。
"## -> /lib/kvlang/规范/runtime语义/两套runtime一致性
}
