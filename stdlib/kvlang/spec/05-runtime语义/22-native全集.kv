// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/native全集 {
	r##"# native builtin 全集（权威）

以下为静态注册表的 opcode 全集（源码运算符号如 `+ - × ÷ % == < ≤ && ! √ …` 映射到对应裸名 opcode）：

**算术 / 数学**：`add`(`+`)、`sub`(`-`)、`mul`(`×`)、`div`(`÷`)、`mod`(`%`)、`pow`、`sqrt`(`√`)、`exp`、`log`、`neg`、`abs`、`sign`、`max`、`min`。

**比较 / 逻辑 / 位**：`eq`(`==`)、`neq`(`!=`/`≠`)、`lt`(`<`)、`gt`(`>`)、`le`(`<=`/`≤`)、`ge`(`>=`/`≥`)、`and`(`&&`)、`or`(`||`)、`not`(`!`)、`bitand`(`&`)、`bitor`(`|`)、`bitxor`(`^`)、`shl`(`<<`)、`shr`(`>>`)。

**类型构造兼转换**（裸名）：`bool`、`int8`、`int16`、`int32`、`int64`、`uint8`、`uint16`、`uint32`、`uint64`、`float32`、`float64`、`char/utf32`、`char/utf8`、`char/ascii`。

**容器构造**（裸名）：`map`（stringkeymap 构造）、`array`（数组构造）、`struct·new`（structref 实例）。

**调试**：`debugger`（等价 `vthread·setstatus("paused")`）。
"## -> /lib/kvlang/规范/runtime语义/native全集
}
