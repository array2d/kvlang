// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/注册表逐槽kindexpr {
	r##"# 注册表：逐槽 kindexpr

扩展的每个 opcode 携带读参表 `rp` 与写参表 `wp`，各槽一个 kindexpr（Rust `runtime-rs/src/rwir/mod.rs::Rwir{rp,wp}`）。注册时各参数类型分别写入 `/lib/<opcode>` 签名行 `[0,x]` 的 `def kindexpr` 槽（`rp` 各参 `x<0` 递减、`wp` 各参 `x>0` 递增）；主槽 body 只记 `[nr][nw][dynamic]`（见 [[总体方案]]）。读参**只读**、写参**可写可读**；扩展**必须**把输出显式写入其写槽（见 [[执行模型]] §五）。示例（networld）：

```
networld/proc·exec   rp=[[]stringkeymap, []stringkeymap]  wp=[uint8, []uint8, []uint8]
networld/fs·read     rp=[[]char/utf8|[]char/utf32, int64, int64]  wp=[[]uint8]
```
"## -> /lib/kvlang/规范/runtime语义/注册表逐槽kindexpr
}
