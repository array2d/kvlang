// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/runtime开发规范 {
	r####"# runtime 开发规范

kvlang 是**小核心 + 扩展主导**语言。中央 runtime 只实现执行核心（取指-解码-执行、
call/return/br/goto、native builtin 派发、`=` 拷贝、用户函数调用）；其余能力（tensor 计算、LLM 调
用、agent 循环、print/fs/http 等 I/O）由**扩展运行时**提供。本章界定核心与扩展之间的契约，以及所有
runtime 实现**必须**遵守的开发规范。

## 扩展运行时模型

**扩展运行时 = 只解析 myrwircaps 内 opcode 的迷你执行器**：从中央 runtime 拿到 PC，连续执行
myrwircaps 内的 rwir，直到遇到不在 myrwircaps 内的指令，把最终 PC 交还中央 runtime。

### 关键术语：myrwircaps（本 runtime 的 rwir 能力表）

**myrwircaps 是每个 runtime 都必须定义的一张 map/table**——登记本 runtime **能就地解释的全部
opcode**，映射 `opcode → 就地处理器`。它是 kvlang runtime 分层与派发的核心术语：一个 runtime「能跑什
么」由且仅由它的 myrwircaps 界定。

- **每个 runtime 必须声明自己的 myrwircaps**。核心 runtime-c 的 myrwircaps 就是其 **native 静态注册
  表**（数组 + 线性查找，见 [[cruntime与后端abi]]），登记 native builtin（+ 控制流 `call`/`return`/
  `br`/`goto` + `=` 拷贝，见 [[runtimec内建]]）。
- **每个扩展运行时各自定义一张 myrwircaps**，登记其自带的 rwir opcode。例：runtime-rs 为 `term`/
  `json`/`http`/`kvlanglayout`/`networld` 等（见 [[notinmyrwircaps]] 就地集合）；未来
  deepx-gpu-compute 为 tensor 融合算子。
- **派发判据**：`opcode ∈ 本 runtime.myrwircaps` → 查表取处理器**就地执行**；否则该 rwir 须经 def
  rwir 路由，**移交**给以它为 myrwircaps 的另一扩展（跨进程 handoff/watch，或嵌入式交回宿主，见
  [[notinmyrwircaps]]、[[cruntime与后端abi_续]]）。实现里 `rwir::is_inproc(op)` 即
  「`op ∈ myrwircaps`」的查表判定。

myrwircaps **只关乎执行位置**（哪张表登记了它、由谁就地跑），不改变 rwir 的动态语义——同一 rwir 无论
落进哪个 runtime 的 myrwircaps，行为必须一致。

**命名规范（normative）**：每个 runtime 在代码中登记自身 rwir 能力的这张表，其标识符/定义**必须**命
名为 `myrwircaps`，不得另起别名（大小写随各语言惯例，如 C/Go `myrwircaps`、Rust 常量
`MYRWIRCAPS`）。审阅任一 runtime 实现时，`myrwircaps` 即「本 runtime 自己实现的 rwir 列表」的唯一入
口名。

核心公理（normative）：

1. **rwir 自描述**：opcode + 读/写参已含执行所需全部信息。
2. **注册即签名**：扩展把 `/lib/<opcode>`（kind=`def rwir`，主槽 body=[nr][nw][dynamic]，各参数类型
   在签名行 `[0,x]` 各槽）写进 kvspace 即完成声明——无 `/sys/`、无中心目录。
3. **批量执行**：扩展连续执行 myrwircaps 内的 rwir 到不在 myrwircaps 内的指令，把最终 PC 写回
   vthread。
4. **被动消费**：跨进程扩展只监控己方 `/lib/<opcode>/vids` 队列。

## 注册与发现

### 签名注册

扩展把每个 opcode 的签名写到 `/lib/<opcode>`（kind=`def rwir`），格式为：

- 主槽 body：`[nr:u16 LE][nw:u16 LE][dynamic:u8]`——读参个数 `nr`、写参个数 `nw`、是否变参
  `dynamic`。
- 签名行 `[0,x]` 各槽：每个参数类型落一个 `def langtype` xvalue（`x<0` 读参、`x>0` 写参、`[0,0]` 为
  锚点）。

例：`/lib/json·to`（nr=1, nw=1）→ 主槽 body=[1,1,0]，`[0,-1]=def langtype("char/utf8")`，
`[0,1]=def langtype("[]char/utf8")`。

- 本 runtime myrwircaps 内 opcode 的签名**不落入 kvspace**——在 C 注册表直接查表（见 [[runtimec内
  建]]）。
- 其它 runtime 的 rwir 签名**落 `/lib/<opcode>`**，中央 runtime 经 `notinmyrwircaps` 识别：
  `opcode[0]=='/'` → false；否则读 `/lib/<opcode>` 的 kind，等于 `def rwir` 即不在本 runtime
  myrwircaps、须经 def rwir 路由。

### 全局标记与幂等

- 裸 opcode（如 `print`/`input`）须在**进程启动期**经 `kvlang_myrwirRegister` 注册全局，使 layout 阶
  段不给它补 `pkg·` 前缀；带 `·` 的限定 opcode（`json·to`）本就不补前缀。
- 注册**必须幂等**：`vids` 队列已存在则跳过。重复注册会破坏既有队列内容（父子 kvlang 共享同一
  redis 时的挂起根因）。

## 注册表：逐槽 langtype

扩展的每个 opcode 携带读参表 `rp` 与写参表 `wp`，各槽一个 langtype（Rust
`runtime-rs/src/rwir/mod.rs::Rwir{rp,wp}`）。注册时各参数类型分别写入 `/lib/<opcode>` 签名行 `[0,x]`
的 `def langtype` 槽（`rp` 各参 `x<0` 递减、`wp` 各参 `x>0` 递增）；主槽 body 只记
`[nr][nw][dynamic]`（见 [[总体方案]]）。读参**只读**、写参**可写可读**；扩展**必须**把输出显式写入其
写槽（见 [[执行模型]] §五）。示例（networld）：

```
networld/proc·exec   rp=[[int64]·[]char/utf32, [int64]·[]char/utf32]  wp=[uint8, []uint8, []uint8]
networld/fs·read     rp=[[]char/utf8|[]char/utf32, int64, int64]  wp=[[]uint8]
```
	"#### -> /lib/kvlang/spec/runtime语义/runtime开发规范
}
