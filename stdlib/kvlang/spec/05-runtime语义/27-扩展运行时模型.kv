// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/扩展运行时模型 {
	r##"# 扩展运行时模型

**扩展运行时 = 只解析己方 opcode 的迷你执行器**：从中央 runtime 拿到 PC，连续执行己方 rwir，直到遇到非己方指令，把最终 PC 交还中央 runtime。

核心公理（normative）：

1. **rwir 自描述**：opcode + 读/写参已含执行所需全部信息。
2. **注册即签名**：扩展把 `/lib/<opcode>`（kind=`def rwir`，主槽 body=[nr][nw][dynamic]，各参数类型在签名行 `[0,x]` 各槽）写进 kvspace 即完成声明——无 `/sys/`、无中心目录。
3. **批量执行**：扩展连续执行己方 rwir 到非己方指令，把最终 PC 写回 vthread。
4. **被动消费**：跨进程扩展只监控己方 `/lib/<opcode>/vids` 队列。
"## -> /lib/kvlang/规范/runtime语义/扩展运行时模型
}
