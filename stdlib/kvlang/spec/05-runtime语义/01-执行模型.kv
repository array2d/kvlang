// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/执行模型 {
	r####"# 执行模型

runtime 是 kvlang 的**动态语义**阶段：对已由 layout 布进 kvspace 的指令做**纯解释执行**。runtime 不编译、不优化、不重排；它只按 PC 逐条取指、解码、执行。存在两套等价实现——C runtime（`kvlang/runtime/`，纯 C + `extern "C"`）与 Rust runtime（`kvlang/runtime-rs/`，二进制名 `kvlang`）——二者必须表现相同的动态语义（差异见 [[cruntime与后端abi]]）。

## 状态皆在 kvspace

runtime **不得**在 kvspace 之外持有执行状态。PC、栈帧、局部变量、调用链一律落 kvspace 的 KV 树；进程可在任意指令边界退出，其执行状态完整留在 kvspace，另起进程按同一 PC 续跑而语义不变（崩溃恢复）。

- **PC 是一个 KV 路径字符串**，形如 `/vthread/<vid>/[s0,s1]`（含嵌套帧时逐段延长，见 §四）。PC 既是取指地址，也是当前帧的定位符。
- **零拷贝读写**：runtime 读值经 `kvspaceGet` 取后端常驻内存的**借用指针**，只读、**不得** `free`；写值经 `kvspaceWriteInPlace`（key 已存在且 body 尺寸不变）或 `kvspaceWriteNewPlace`（新建 / 换 kind / 换尺寸）向后端要可写偏移指针后直接写字节。runtime **不得**自持堆缓冲中转整条 TLV。

## 坐标系

一条指令在其所属函数子树内由二元坐标 `[s0,s1]` 定位：`s0` 为指令行（行 0 为签名，指令从行 1 起），`s1` 为参数轴（0 为 opcode，负数为读参绑定 `[0,-k]`，正数为写参绑定 `[0,k]`）。签名槽 `[0,0]` 为 opcode/签名行锚点；其所属函数目录主槽的 langtype 为 `rwfunc`（用户函数）或 `def rwir`（rwir 路由头，见 [[rwirext扩展]]；见 [[总体方案]]）。

## 指令解释循环

runtime 对一个 vthread 的执行是如下循环（C 实现 `runtime/src/kvcpu.c:kvlangKvcpuExecuteMode`）：

1. **状态门**：读该 vthread 的 `‥status`。仅当状态为 `init`、`running`、`wait` 之一时**继续**；其余状态（`paused` / `done` / `error` 等）即**停机**，进程可正常退出。
2. **深度检查**：帧深超过实现上限（`MAX_STACK_DEPTH`）时置 `RecursionError` 并停该 vthread。
3. **取指解码**：由 PC 定帧根、经帧的 `‥lib` 软链定位指令树，`RwirDecode` 稠密探查槽位、遇空即停，解出 opcode 与读/写参。空 opcode 视为 `/lib` 损坏或跳转越界，报 `RuntimeError` 停该 vthread。
4. **派发执行**（见 §五）。
5. **推进 PC**：多数指令执行后由自身写回新 PC（`NextPc` 求后继，或 call/return/br/goto 改写 PC）；循环回到 1。

## 派发链

每条指令按**固定优先级**派发，命中即止：

1. **控制指令**（`op_is_control`）——`call` / `return` / `br` / `goto`。
2. **native builtin**（`kvlangBuiltinIsNative`）——静态注册表内的核心算子（枚举见 [[函数调用与内建]]）。
3. **`=` 拷贝**（`is_copy_op`）——值拷贝语义。
4. **须经 def rwir 路由的 rwir**（`isothersrwir`）——`/lib/<opcode>` 存在且 kind=`def rwir`（见 [[rwirext扩展]]）。
5. **否则视为用户函数调用**——opcode 移入 `reads[0]`，改写为 `call` 指令。

实现**必须**遵循此优先级：native 注册表先于 `/lib` 查表，故与 native 同名的 `/lib/<op>` 不改变派发。

## 调用即无返回值——输出走写参映射

kvlang 的调用**没有返回值表达式**。函数/rwir 的输出一律经**写参映射** `f(a) -> r` 显式落到调用者指定的写槽；一条指令可有零个或多个写参。

- **call**：创建子帧 `/vthread/<vid>/<父帧路径>/[s0,0]/`，把实参经 Ptr 链绑定进子帧的读参槽 `[0,-k]`、写参槽 `[0,k]`（零拷贝跨帧传参，不复制实参内容），子帧记 `‥callpc`（发起调用的 PC）与 `‥returnpc`（返回后继续的 PC），PC 进入子帧首指令。
- **return**：弹出当前帧、删其子树；顶层帧返回则整个 vthread 结束（新 PC 为空）；否则新 PC 取本帧 `‥returnpc`。返回链断裂（帧无 `‥returnpc`）**必须**报 `RuntimeError` 并保留该帧供排查。
- 读参**只读**、写参**可写可读**；修改型指令**必须**把结果显式写入其写槽（见 [[函数调用与内建]]）。
	"#### -> /lib/kvlang/spec/runtime语义/执行模型
}
