// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/print不是native {
	r##"# `print` / `println` / `cerr` / `input` 不是 native

**规范要点**：`print`、`println`、`cerr`、`input` **不在** native 注册表——它们是 **term runtime 的 rwir（经 def rwir 路由，`/lib/<opcode>` kind=`def rwir`）**，经派发链第 4 级 `isothersrwir` 命中（见 [[rwirext扩展]]）。term 扩展与中央 runtime 同进程，就地 `ResolveRead` + display 直写宿主 stdout/stderr。实现**不得**把这四个 opcode 当 native 处理。
"## -> /lib/kvlang/规范/runtime语义/print不是native
}
