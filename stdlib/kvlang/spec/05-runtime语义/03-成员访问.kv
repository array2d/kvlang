// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/成员访问 {
	r####"# 成员访问（运行期语义）

本章界定运行期对复合数据的访问语义：成员符 `·` 的解引用规则、动态键、指针的单跳类型检查、容器 memindex 的运行期行为、compact 数组下标。类型的静态形态（种类、kindexpr、数组形态）见类型系统卷；本章只讲 runtime **如何在 kvspace 中定位与读写**这些结构。

## 两个索引符号

kvlang 用两个符号访问复合数据，语义严格分离，runtime **不得**混用：

| 符号 | 目标 | 说明 |
|------|------|------|
| `a[i]` | **数组下标** | compact 数组的元素访问，`i` 为整数 |
| `base·name` | **容器成员 / 限定名** | `·`（U+00B7，中点）是成员访问符 |

容器成员一律走 `·`（或 `kv·get`/`kv·set`）；`[]` 只索引 compact 数组。

## 容器 = 值 + memindex

复合命名数据（`stringkeymap`、`struct`）在 kvspace 中的运行期表示为**值 + memindex**：`base` 键持容器值（含 dims、容量等头信息），成员 `base·name` 作为**独立子 key** 落库。`·`（memindex 标记）分出容器的成员空间；memindex 的成员坐标名（`stringkeymap` → stringkeymap index、`struct` → struct index）是成员集合的唯一权威，遍历成员**必须**据此枚举，**不得**假设成员键连续或有序之外的布局。

- **写成员**：`rec·name = 值` 的写槽是完整成员键 `rec·name`，`=` 为值拷贝。
- **读成员**：`rec·name -> v` 直接取 key `rec·name`；缺失成员的动态读（`kv·get`）返回 `None`。

## `·` 的解引用：按值优先，按名回退

`base·name`（读写同规则）中 `base` 的解析在 runtime 按以下顺序确定成员键：

1. **按值解引用**：`base` 持有非空字符串值（路径指针）时，成员键 = `值(base)·name`。例：`"/n1" -> p` 后 `p·val` 解析为 `/n1·val`。
2. **按名回退**：`base` 无值（或非字符串）时，成员键 = `解析(base)·name`；`解析()` 帧感知——裸名 → `帧根/base`，`/` 开头 → 直通绝对路径。

该规则让"局部结构体"与"指针解引用"共用一套语法：结构体的 `base` 保持无值（走按名），指针变量存路径字符串（触发按值）。跨函数共享的数据**应**放绝对路径（`/n1`），使不同帧的指针都能解引用到同一 key。

## 动态成员名 `·*`

`base·*k` 用 `k` 的**值**作为成员名：读 `m·*k` 即以局部变量 `k` 当前的字符串值定位 `m·<k值>`。`·*` 用于局部容器变量的动态键访问；等价的显式形式为 `kv·get(m, k)` / `kv·set(m, k, v)`。

## 指针：完整 kindexpr + 单跳类型检查

指针（`Ptr`，head ref==1，源码前缀 `*`）在 kvspace 中存**目标的完整 kindexpr + 目标 key**（`kvspaceNewPtr` 是唯一构造入口，runtime **不得**自造 Ptr 编码）。

- **单跳类型检查**：写入指针时，实现**必须**校验指针声明的目标 kindexpr 与被指向 key 的实际 kindexpr 一致——只查一跳，不递归展开链。
- **读解引用链**：读参解析（`resolve_read_path`）从命名 Ptr 出发，只追显式 `Ptr`（ref==1）链到非指针值即停；**不得**把 char 字符串值误当路径继续追（否则字符串实参内容会被当键，穿两层调用即变 `None`）。写侧解析（`ResolveWriteSlot`）遵循同一纪律。

## 扩展世界句柄（ref=2，@ext）

`ref` 是 head 中的**独立 u8 字段**（0=inline、1=ptr、2=@ext），不是 kindexpr/kind 首字符；源码类型标注中 `*`/`@` 前缀由 layout 剥离落成 `ref` 字节，wire 层 langtype 串不含前缀（见 [[总体方案]]）。ref=2（@ext）表示真实值在扩展世界，head 的 langtype 仍为目标形态的完整 kindexpr 串（不带 `@` 前缀），body 不是真实值字节，而是**定位串**（如 `/networld/<host>/proc/<pid>/stdout`）；读取该 key 时 runtime 按 body 前缀路由到对应扩展兑现器还原真实字节（见 [[rwirext扩展]]、[[执行模型]] §八）。真实 xvalue 落在扩展世界（fs 文件、gpu tensordata 等），非 kvspace 元存。

## compact 数组下标

数组有两种存储形态：**compact**（`[...]` 字面量，元素连续打包进单个 XValue，head kind = 元素类型）与 **stringkeymap**（`{...}` 字面量，散 key，每元素落 `base·i` 独立子 key）。下标 `a[i]` / `xv·at` / `xv·set` 用于 **compact** 形态；stringkeymap 形态用 `for-in` / `kv·get` 遍历访问。

- **下标读**：`a[i] -> e`（layout 脱糖为 `xv·at`/`kv·get`），按 `kvlangXvalueElemSize(kind)` 定的元素字节宽零拷贝定位第 `i` 个元素。
- **下标写**：`a[i] = v`（脱糖为 `xv·set`），就地改第 `i` 个元素。写下标**必须**用赋值式 `a[i] = v`；`->` 形式的写槽不得带下标（`v -> a[i]` 由 layout 拒绝）。
- `xv·at(a, i) -> e` / `xv·set(a, i, v) -> a` 是显式等价形式。

元素字节宽由 kind 决定（`char/utf8`=1，`int32`/`float32`=4，`int64`/`float64`=8……），数组与字符串的元素访问据此零拷贝定位。锚例：`tutorial/04-ndarray/`（`continuous.kv`、`subscript_write.kv`、`separated.kv`、`string_array.kv`）、`tutorial/05-dict/`、`tutorial/12-struct/`。
	"#### -> /lib/kvlang/spec/runtime语义/成员访问
}
