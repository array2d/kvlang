// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/rwirext扩展 {
	r##"# rwirext 扩展运行时

kvlang 是**小核心 + 扩展主导**语言。中央 runtime 只实现执行核心（取指-解码-执行、call/return/br/goto、native builtin 派发、`=` 拷贝、用户函数调用）；其余能力（tensor 计算、LLM 调用、agent 循环、print/fs/http 等 I/O）由**扩展运行时**提供。本章界定核心与扩展之间的契约。

## 一、扩展运行时模型

**扩展运行时 = 只解析己方 opcode 的迷你执行器**：从中央 runtime 拿到 PC，连续执行己方 rwir，直到遇到非己方指令，把最终 PC 交还中央 runtime。

核心公理（normative）：

1. **rwir 自描述**：opcode + 读/写参已含执行所需全部信息。
2. **注册即签名**：扩展把 `/lib/<opcode>`（kind=`defrwir`，空函数体，仅签名）写进 kvspace 即完成声明——无 `/sys/`、无中心目录。
3. **批量执行**：扩展连续执行己方 rwir 到非己方指令，把最终 PC 写回 vthread。
4. **被动消费**：跨进程扩展只监控己方 `/lib/<opcode>/vids` 队列。

## 二、注册与发现

### 签名注册

扩展把每个 opcode 的签名写到 `/lib/<opcode>`（kind=`defrwir`），定义体格式为：

```
[nr:u16 LE][nw:u16 LE][读参 kindexpr … 写参 kindexpr，"\n" 连接]
```

例：`/lib/json·to → defrwir(nr=1, nw=1, "char/utf8\n[]char/utf8")`。

- native builtin 的签名**不落盘**——在 C 注册表直接查表（见 [[函数调用与内建]]）。
- 扩展签名**落 `/lib/<opcode>`**，中央 runtime 经 `isothersrwir` 识别：`opcode[0]=='/'` → false；否则读 `/lib/<opcode>` 的 kind，等于 `defrwir` 即扩展 rwir。

### 全局标记与幂等

- 裸 opcode（如 `print`/`input`）须在**进程启动期**经 `kvlang_rwirextRegister` 注册全局，使 layout 阶段不给它补 `pkg·` 前缀；带 `·` 的限定 opcode（`json·to`）本就不补前缀。
- 注册**必须幂等**：`vids` 队列已存在则跳过。否则脏 kvspace 上重复注册时，Set 经路径穿透会把首队列改成自指 `Ptr`，令 `resolve_path` 死循环（父子 kvlang 共享同一 redis 的挂起根因）。

## 三、注册表：逐槽 kindexpr

扩展的每个 opcode 携带读参表 `rp` 与写参表 `wp`，各槽一个 kindexpr（Rust `runtime-rs/src/rwir/mod.rs::Rwir{rp,wp}`）。注册时 `rp ++ wp` 以 `\n` 连接为签名串写入 `defrwir`。读参**只读**、写参**可写可读**；扩展**必须**把输出显式写入其写槽（见 [[执行模型]] §五）。示例（networld）：

```
networld/proc·exec   rp=[[]stringkeymap, []stringkeymap]  wp=[uint8, []uint8, []uint8]
networld/fs·read     rp=[[]char/utf8|[]char/utf32, int64, int64]  wp=[[]uint8]
```

## 四、handoff 协议（跨进程扩展）

跨进程扩展（如 Go json、Py numpy、op-gpu）经共享队列 handoff：

```
/lib/<opcode>/vids/<vtid> = pc   # 中央 runtime 写，扩展消费（strkeymap 队列）
```

第一个 rwir 的 `vids` 是真实 strkeymap；后续同 opcode 的 `vids` 是 `Ptr` 指向第一个（共享同一队列）。时序：

1. 中央 runtime `isothersrwir` 命中 → `handoff_external_rwir`：`Set /lib/<op>/vids/<vtid>=pc`。
2. 扩展发现队列中的 vtid，`RunSeq(pc)`：从 PC 起逐条解码，只要 opcode ∈ 己方集合就执行，遇第一条非己方指令即停（**顺序执行，非并行 batch**）；写回最终 PC，删 `vids` 项。
3. 中央 runtime `Watch` 直到 vtid 从队列消失（30s 超时），继续。

中央 runtime **不推进 PC**——扩展批量执行后已写回最终 PC，中央直接读新 PC 续跑。

## 五、就地扩展（同进程）

同进程扩展在驱动循环里就地派发（Rust `run_vid`：`rwir::is_inproc(op)` 命中 → `rwir::dispatch`），无 handoff、无 watch。当前就地集合（`runtime-rs`）：

```
term      print / println / cerr / input
json      json·to / json·from
http      http·call
layout    kvlanglayout·vet / ·format / ·layout / ·dump
networld  networld/proc·exec
          networld/fs·size / ·read / ·write / ·append / ·list / ·del / ·mkdir / ·exists
```

就地与跨进程只是**执行位置**之别，职责线一致——两者都只经 `kvlang_rwirext*` 解析参数、只经 `kvspace*` 正典 ABI 读写编解码。

## 六、参数解析与零拷贝读

扩展**只经** `kvlang_rwirext*` 宿主 ABI 拿参数，**不得**自行解析 rwir 槽位：

- `Params(pc)`：返回 opcode + 读/写参名（`\n` 分隔）。
- `ResolveRead(pc, idx)`：读参解析为**字符串**（stringkeymap 值展开为 `[e0, e1, …]`）；**对数组只返首元素**。
- `ResolveReadPath(pc, idx)`：读参解析为**KV 路径**（变量/临时 → 帧槽路径；绝对路径 → 直接返回；内联字面量 → 空串）——供**零拷贝读整块 ndarray**：拿到容器路径后经 `kvspaceGet` 取 TLV、`DecodeHead` 定位 body 段直接读字节。
- `ResolveWrite(pc, idx)`：写参解析为 KV 路径。
- `NextPc(pc)`：纯函数，求后继 PC。

**规范要点**：需要整块字节（如 `[]uint8`）时**必须**走 `ResolveReadPath` + TLV body，**不得**用 `ResolveRead`（后者对数组只返首元素）。

## 七、扩展世界 `@` 前缀句柄

扩展的输出可以不是 kvspace 元存里的真实字节，而是**扩展世界句柄**：写槽落 kind=`@<目标kindexpr>`（head ref==2），body 为**逻辑定位串**（如 `/networld/<host>/proc/<pid>/stdout`）。读取该 key 时按 body 前缀路由到对应 `/lib/networld/*` 兑现器还原真实字节（如 proc 兑现器把逻辑路径映射到 `/tmp/kvlangruntime-rs/<pid>/<stream>` 物理文件读回）。真实 xvalue 落在扩展世界（fs 文件、gpu tensordata），非 kvspace 元存。锚例：`tutorial/14-networld/05-capture.kv`。

## 八、核心 / 扩展边界（不可越界的职责线）

扩展回答的唯一问题是「**算什么、怎么算**」；它**不回答**「值长什么样、指令怎么解码、类型怎么校验」——这些是 runtime-c 的值语义（见 [[cruntime与后端abi]]）。扩展只有三个合法动作：

1. **拿 PC**：经 handoff / 驱动循环取当前 PC，跑己方 rwir，`NextPc` 求后继。
2. **解析参数**：**只经** `kvlang_rwirext*`（§六）。
3. **读写 kvspace**：**只经** `kvspace*` 正典 ABI（`Get` 借用读、`WriteInPlace`/`WriteNewPlace` 写、`TlvEncode`/`DecodeHead` 编解码）。**绝不自造 kindexpr 构造器、绝不手写 TLV/head。**

唯一保留的例外是 json 反序列化里的 kindexpr **串解析器**：kvspace 只导出编码器、不导出串解析器，而「把外部文本按类型/形状分发写回子树」是扩展**自身的反序列化职责**，故留在扩展侧，且只读 head、不造 head。这条例外恰印证边界：**能调用 kvspace 的一律调用；kvspace 未提供且属扩展自身语义的才自理。**

## 九、`/lib` 与 `/ext` 拓扑

| 目录 | 回答 | 内容 |
|------|------|------|
| `/lib/<opcode>` | 有什么 op（签名） | `defrwir` 签名（读/写参 kindexpr） |
| `/ext/` | 在哪、怎么执行（拓扑） | 存储 / 计算 / 通信引擎注册 |

无状态扩展（term/json）只写 `/lib/`；有状态扩展引擎（op-gpu 等）写 `/lib/` + `/ext/`，handoff 协议一致，后者多一次 `/ext/` 查表定位引擎。
"## -> /lib/kvlang/规范/runtime语义/rwirext扩展
}
