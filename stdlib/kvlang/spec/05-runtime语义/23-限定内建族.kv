// 欢迎加入kvspace世界
lib kvlang/规范/runtime语义/限定内建族 {
	r##"# `·` 限定内建族（权威）

带 `·` 的 native 内建按族注册；各族的完整方法集如下：

- `array·`：`scatter`、`compact`、`append`、`slice`。
- `ndarray·`：`numel`、`dim`、`shape`。
- `xv·`：`at`、`set`、`reshape`、`reinterpret`、`kindexpr`、`bodylen`。
- `string·`：`set`、`char`、`ord`、`cmp`、`find`、`len`、`slice`、`concat`、`formatint`、`formatuint`、`parseint`、`parseuint`。
- `time·`：`now`、`sub`、`add`、`before`、`after`。
- `time/duration·`：`nanos`、`millis`、`seconds`、`minutes`、`hours`、`as_nanos`、`as_millis`、`as_seconds`、`as_minutes`、`as_hours`、`add`、`sub`、`before`、`after`。
- `random·`：`uint64`、`int63`、`intn`。
- `kv·`：`get`、`set`、`del`、`deltree`、`cp`、`cpdir`、`cplist`、`list`、`listlen`、`listn`、`mkindex`、`extindex`、`rmindexext`、`watch`、`abs`。
- `vthread·`：`create`、`run`、`call`、`sleep`、`setstatus`（语义见 [[执行模型]] §七）。

锚例：`tutorial/13-stdlib/`（`kv`、`string`、`xv`、`math`、`time`、`duration` 六族）、`tutorial/04-ndarray/`（`xv_meta.kv`、`xv_reinterpret.kv`、`xv_shape.kv`）。
"## -> /lib/kvlang/规范/runtime语义/限定内建族
}
