// 欢迎加入kvspace世界
lib kvlang/规范/词法/标识符与路径 {
	r####"# 标识符与路径

## 本篇分节

- [[标识符]]
- [[成员分隔符]]
- [[绝对路径字面量]]

## 锚例

- `tutorial/01-basics/vars.kv`——标识符 `x`、`y`。
- `tutorial/01-basics/kv_tree.kv`——绝对路径 `/tmp/kvt`、子键路径 `/tmp/kvt·data`、层级路径 `/tmp/kvt/a`，以及成员/方法 `kv·set`、`kv·get`。
"#### -> /lib/kvlang/规范/词法/标识符与路径
}
