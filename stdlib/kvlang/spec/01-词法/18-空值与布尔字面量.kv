// 欢迎加入kvspace世界
lib kvlang/规范/词法/空值与布尔字面量 {
	r####"# 空值与布尔字面量

- `None`——空值字面量。kvlang 的空值**只有** `None`；书写 `null` 是错误，layout **必须**显式拒绝，**不得**当作裸标识符静默放行。未赋值的变量地址即读作 `None`。
- `true` / `false`——布尔字面量。

三者均为**上下文词**：词法阶段化为标识符，由 layout/runtime 依类型上下文解释为对应值。
"#### -> /lib/kvlang/规范/词法/空值与布尔字面量
}
