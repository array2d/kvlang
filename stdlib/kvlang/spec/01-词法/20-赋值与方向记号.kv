// 欢迎加入kvspace世界
lib kvlang/规范/词法/赋值与方向记号 {
	r####"# 赋值与方向记号

kvlang 有三种赋值/方向记号，均词法化为 `Arrow`：

- `=`——写入左侧，等价于 `<-`；
- `<-`——写入左侧（数据从右流向左）；
- `->`——写入右侧（数据从左流向右）。

```
x <- 42
y =  x + 8   // = 等价于 <-
42 -> x      // 写入右侧
```

`=` 仅为写入记号，kvlang **无**独立的等号赋值语义之外用法；相等比较写作 `==`。
"#### -> /lib/kvlang/规范/词法/赋值与方向记号
}
