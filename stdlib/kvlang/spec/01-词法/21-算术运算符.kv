// 欢迎加入kvspace世界
lib kvlang/规范/词法/算术运算符 {
	r####"# 算术运算符

| 字形 | 含义 | 一元 |
|------|------|------|
| `+`  | 加   | 是（一元正） |
| `-`  | 减   | 是（一元负） |
| `×`  | 乘（U+00D7） | 否 |
| `÷`  | 除（U+00F7） | 否 |
| `%`  | 取模 | 否 |

**乘法是 `×`、除法是 `÷`**，非 ASCII 的 `*` 与 `/`：

- `*`（U+002A）**不是**乘号。它在符号表中登记为 `pointer`，非算术运算符。
- `/`（U+002F）**不是**除号。它只用于绝对路径字面量与注释（`//`、`/*`）；出现在其他位置的裸 `/` 不承载算术除法语义。

一元 `+`、`-` 作前缀；`√`（U+221A，平方根）亦为一元前缀运算符。
"#### -> /lib/kvlang/规范/词法/算术运算符
}
