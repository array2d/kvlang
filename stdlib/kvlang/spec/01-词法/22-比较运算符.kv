// 欢迎加入kvspace世界
lib kvlang/规范/词法/比较运算符 {
	r####"# 比较运算符

```
==   !=   <   >   <=   >=
```

其中三者另有等价的 Unicode 字形别名，词法上与 ASCII 形式同义：

- `!=` ≡ `≠`（U+2260）
- `<=` ≡ `≤`（U+2264）
- `>=` ≡ `≥`（U+2265）
"#### -> /lib/kvlang/规范/词法/比较运算符
}
