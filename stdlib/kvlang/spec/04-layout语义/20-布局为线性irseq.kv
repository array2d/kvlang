// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/布局为线性irseq {
	r##"# 布局为线性 irseq

lower 之后，layout 把**所有**块体展平进函数目录下的**单一线性指令序列** `[1,0]…[N,0]`——**不建 scope 子目录**。goto/br 的跳转目标在布局时解析为**整数 irseq**（目标块首指令的 s0 序号），写入其读槽。

标签到 irseq 的映射另存于 `/lib/<pkg>·<name>/‥labels/<label>`（kind `int64`），供审查与工具还原。

示例（`while (i <= n) { total + i -> total; i <- i + 1 }`，其中 `n` 为读参）布出：

```
[3,0]="goto"  [3,-1]=4              # 跳到条件块（irseq 4）
[4,0]="int64·le"  [4,-1]="i" [4,-2]="n"  [4,1]="_1"
[5,0]="br"  [5,-1]="_1" [5,-2]=6 [5,-3]=9   # 真→6(循环体) 假→9(出口)
[6,0]="int64·add" [6,-1]="total" [6,-2]="i" [6,1]="total"
[7,0]="int64·add" [7,-1]="i" [7,-2]=1 [7,1]="i"
[8,0]="goto" [8,-1]=4               # 回条件块
[9,0]="return"                      # 出口块（补 return）
‥labels/_while_2=4  ‥labels/_do_3=6  ‥labels/_exit_4=9
```

> 注：早期 Go 实现（及仓库中部分 `.kv.dump` 快照）把每块布为 `<scope>[i,j]` scope 子目录、goto/br 存字符串标签。当前 Rust layout **不**如此——统一展平为线性 irseq + 整数目标。runtime 帧模型见 [[执行模型]]。
"## -> /lib/kvlang/规范/layout语义/布局为线性irseq
}
