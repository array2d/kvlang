// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/可见性 {
	r##"# LOG_LEVEL 可见性

环境变量 `LOG_LEVEL`（默认 `warn`）控制诊断可见性：

| LOG_LEVEL | 可见 |
|-----------|------|
| `debug` | debug/info/warn/error |
| `info` | info/warn/error |
| `warn`（默认） | warn/error |
| `error` | error |
"## -> /lib/kvlang/规范/layout语义/可见性
}
