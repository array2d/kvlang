// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/lib与rwir数据 {
	r##"# lib 与 rwir 数据

`/lib` 树即 kvlang 的**全局命名空间**——无 `import`，跨 lib 调用一律走全路径 `/lib/{pkg}·{func}()`。本章界定 layout 往 `/lib` 写入的各类内容、其 KV 布局与 kind、以及扩展 rwir 的表示。

## `/lib` 下的四类内容

| 类别 | 语义 | 有指令体 | KV kind | 布局路径 |
|------|------|---------|---------|---------|
| **rwfunc** | 用户函数 | 是 | `rwfunc`（签名槽 `[0,0]`） | `/lib/<pkg>·<name>/` |
| **rwir 声明** | 扩展/用户声明的 rwir | 否，仅签名 | `defrwir` | `/lib/<opcode>` |
| **lib 数据** | 常量 | 否，单 XValue | 值类型 | `/lib/<name>` |
| **struct 原型** | 结构体原型 | 否 | `struct` + memindex | `/lib/<Name>` |

四类同存 `/lib`，无独立 `/rwir/` 命名空间。

## pkg 与嵌套 lib

pkg **严格取自 `lib` 声明**（非文件名）。嵌套 lib 的 pkg 以 `/` 累积，函数名以 `·` 附于末段：

```
lib a { lib b { rwfunc f() -> (r:int64) { 1 -> r } } }
→ /lib/a/b·f/[0,0]
```

无 lib 包裹的顶层 rwfunc 登记为 `/lib/<name>`（匿名 pkg）。

## init 函数

裸顶层语句与各 lib 的 `init` 体合并为 init 函数（无 `init` 关键字亦自动合成）：空 pkg → `init`，`lib X { }` → `X·init`。跨 lib 调用示例：`math·sum(3, 4) -> s`。

## rwfunc 签名 / defrwir 定义体

rwfunc 签名槽 `[0,0]`（kind `rwfunc`）与 rwir 声明 `/lib/<opcode>`（kind `defrwir`）**共用定义体格式**：

```
[nr:u16 LE][nw:u16 LE][各参 kindexpr 以 "\n" 连接]
```

区别仅两点：kind 不同；rwfunc 签名有指令体子节点（`[i,j]`，i≥1）、defrwir 无。rwfunc 的 array_len 记指令条数。

## rwfunc 布局细节

`write_func` 往 `/lib/<pkg>·<name>/` 写：

- `[0,0]`——rwfunc 签名。
- `.src`（ASCII 点后缀，`/lib/<pkg>·<name>.src`）——源码副本，kind `char`。
- 每个命名参数——`funcDir/<param>` 存 Ptr（kind `char`）指向槽坐标串（读参 `[0,-j]`、写参 `[0,+j]`）。
- `[i,j]`（i≥1）——线性指令（见 [[控制流]]）。
- `‥labels/<label>`——标签到 irseq 映射（若有控制流）。

## rwir 声明与扩展 rwir

`/lib/<opcode>`（kind `defrwir`）由两条路径写入：

- **用户 rwir 声明**——源码 `rwir myop(A:int64) -> (C:int64)`（无体）由 layout `write_rwir_decl` 写入。
- **扩展 rwir 注入**——宿主经运行时注册接口（`kvlang_rwirextRegister(opcode, nr, nw, sig)`，sig 为各参 kindexpr 以 `\n` 连接）写入，注册前校验各参 kindexpr。

layout **不**判定某 opcode 是 native、扩展还是用户 rwfunc——调用目标 opcode 槽统一 kind `rwir|rwfunc`，判定推迟到 runtime 查 `/lib/<opcode>` 的 XValue kind（见 [[执行模型]]）。扩展 rwir 仅以 `/lib/<opcode>` 的 defrwir 出现，没有 native/user 的静态归类。

## native rwir 不落盘

`+`、`==`、`array`、`sqrt`、`print` 等 native builtin 融合在 runtime 内建表，**不**写 kvspace、运行时也不回查 `/lib`。数值多类型算子在内建表中融合为单条（`add` 覆盖 int8…float64），派发前剥掉 `<numkind>·` 前缀，输出 kind 由实参决定。

## lib 数据（常量）

`/lib/<name>` 存单个 XValue 常量，以绝对路径读写。`lib` body 内的绝对路径赋值即写常量：

```
lib math {
    /lib/math·Pi = 3.141592653589793
    /lib/math·E  = 2.718281828459045
}
```

任意 kvlang 代码以绝对路径访问 `print(/lib/math·Pi)`。裸名赋值（`Pi = 3.14`）是 `/vthread` 帧局部变量、跑完回收，**不**入 `/lib`。当前实现**不**内置 stdlib 自举；常量由用户或宿主显式 layout 写入。

## struct 原型

`struct` 声明布进 `/lib/<Name>`：基节点（kind `struct`，body 为各字段 `name:kindexpr`）+ memindex（`/lib/<Name>·`，字段名唯一权威）+ 各字段默认值。runtime `struct·new` 以此原型克隆实例，字段默认值即实例初值。
"## -> /lib/kvlang/规范/layout语义/lib与rwir数据
}
