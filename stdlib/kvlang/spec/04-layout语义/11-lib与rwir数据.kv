// 欢迎加入kvspace世界
lib kvlang/spec/layout语义/lib与rwir数据 {
	r####"# lib 与 rwir 数据

`/lib` 树即 kvlang 的**全局命名空间**——无 `import`，跨 lib 调用一律走全路径 `/lib/{pkg}·{func}()`。
本章界定 layout 往 `/lib` 写入的各类内容、其 KV 布局与 langtype；def rwir 路由头由各 runtime 注册
（非 layout 写入），其表示一并在此界定。

## `/lib` 下的四类内容

| 类别 | 语义 | 有指令体 | langtype | 布局路径 |
|------|------|---------|---------|---------|
| **rwfunc** | 用户函数 | 是 | `rwfunc` | `/lib/<pkg>·<name>/` |
| **def rwir 路由头** | rwir 统一调度入口，各 runtime myrwircaps 对外入口 | 否，无 kvlang 实现体 | `def rwir` | `/lib/<opcode>` |
| **lib 数据** | 常量 | 否，单 XValue | 值类型 | `/lib/<name>` |
| **struct 原型** | 结构体原型 | 否 | `def struct` | `/lib/<Name>` |

四类同存 `/lib`，无独立 `/rwir/` 命名空间。

## pkg 与嵌套 lib

pkg **严格取自 `lib` 声明**（非文件名）。嵌套 lib 的 pkg 以 `/` 累积，函数名以 `·` 附于末段：

```
lib a { lib b { rwfunc f() -> (r:int64) { 1 -> r } } }
→ /lib/a/b·f/[0,0]
```

无 lib 包裹的顶层 rwfunc 登记为 `/lib/<name>`（匿名 pkg）。

## init 函数

裸顶层语句与各 lib 的 `init` 体合并为 init 函数（无 `init` 关键字亦自动合成）：空 pkg → `init`，
`lib X { }` → `X·init`。跨 lib 调用示例：`math·sum(3, 4) -> s`。

## 签名装载：计数头 + 参数类型承载

rwfunc（layout 装载）与 def rwir（runtime 注册）的签名都分两处存放（见 [[总体方案]]）：

1. **计数头**：`rwfunc` 落签名行锚点 `/lib/pkg·funca/[0,0]`（langtype `rwfunc`）、`def rwir` 落
   `/lib/<op>` 节点自身（langtype `def rwir`），其 body 只记 `[nr:u16 LE][nw:u16 LE][dynamic:u8]`——
   读参个数 `nr`、写参个数 `nw`、是否变参 `dynamic`（末位读参 `...`）。**不含任何参数 langtype 串**。
2. **参数类型承载**（按符号分两路，见 [[storetype_def族与签名行]]）：`def rwir` 落位置化签名行
   `[0,x]` 槽（`x<0` 读参、`x>0` 写参、`[0,0]` 为锚点），每槽一个 `def langtype` xvalue、body 为该参
   数完整 langtype 串；`rwfunc` 落**参数定义键** `funcDir.[0,±j]`（点后缀，与坐标斜杠键不同名），
   body = 参数名 + NUL + 类型串。

两者区别：rwfunc 目录下有实现指令行子节点（`[s0>0,·]`，s0≥1）；def rwir 无实现指令行。
	"#### -> /lib/kvlang/spec/layout语义/lib与rwir数据
}
