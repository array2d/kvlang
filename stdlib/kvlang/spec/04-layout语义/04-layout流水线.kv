// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/layout流水线 {
	r##"# layout 流水线

layout 是 kvlang 的静态语义阶段：**语法检查 + 布局 + 降级**。它把源码检查、降级，并把结果写进 kvspace 的 `/lib/` 子树，供 runtime 解释执行。layout 是 **o0 编译器**——做检查、布局与降级，**不做任何优化**：任何优化都会丢失高层语义，使扩展编译器无法按原语义在异构硬件上优化，故优化一律留给扩展编译器（见 [[范围与一致性]]）。实现是独立 Rust crate `kvlang/layout/`，产 `kvlanglayout` 可执行文件与 `libkvlanglayout` 动态库。

## 流水线阶段

```
源码 → scanner（scan）→ [Token]
     → parser（parse_code）→ ast::File   （递归下降，累积全量诊断）
     → lower（lower_func）  控制流降级 + 类型推断 + 特化
     → write_func           AST → KV 指令树 /lib/<pkg>·<name>/[i,j]
```

关键性质：

- **PC 是 KV 路径字符串**，不是整数偏移。
- **指令存储在 KV 树**中，经 `kv·get` 逐槽读取；无字节码数组。
- 换行是语句分隔符（`Newline` token），`{ }` 负责块结构；块结构由消费 `LBrace`/`RBrace` 追踪。
- 诊断**不首错即止**：解析器累积全部诊断，最后统一判定（见 [[诊断]]）。

## compile 的处理顺序

顶层 `compile` 依次处理：`struct` 声明 → `rwfunc`（各自 lower + write_func）→ `rwir` 声明 → **init**。裸顶层语句与各 `init` 体合并为 init 函数：空 pkg → `init`，`lib X { }` → `X·init`。无任何可执行代码报 `no executable code found`。compile 返回本次写入的 init 函数名列表（供消费方按 lib 声明驱动 init，非文件系统路径推导）。

## write_func 步骤

1. 推断类型并特化算子。
2. `del_tree` 本函数目录（增量覆盖，仅本函数子树）。
3. 写 `[0,0]` 签名（rwfunc）、`.src` 源码副本、各命名参数 Ptr。
4. 从 `[1,0]` 起写线性指令；goto/br 标签解析为整数 irseq。
5. 若有标签，写 `‥labels/<label>` → irseq。

## C ABI 入口

layout 能力经 cdylib 导出，第三方无需 fork 子进程即可布局（各入口都用 catch_unwind 兜住内部 panic，坏输入只返回 -1）：

| 符号 | 职责 |
|------|------|
| `kvlangLayoutFile(path, dsn, …)` | 读 `.kv` 文件 layout 进 kvspace |
| `kvlangLayoutCode(src, dsn, …)` | 内存源码串 layout 进 kvspace |
| `kvlangLayoutVet(src, …)` | 只校验（parse + lower），不写 kvspace |
| `kvlangLayoutFormat(src, …)` | 格式化（parse → 规范化源码），不写 kvspace |
| `kvlangLayoutDump(lib, dsn, …)` | 把 `/lib` 子树逆向重建为可运行 kvlang + 槽位注释 |
| `kvlangKindexprParse(kindexpr, out)` | 解析 XValue head 的 kindexpr（唯一事实源，供各消费方复用） |

成功返回 0（`entry_out` 为 init 入口名列表，`\n` 分隔），失败返回 -1（`err_out` 为错误）。`kvlangLayoutFile` 是 `kvlangLayoutCode` 的薄封装。

## vet 闸门

`vet` 走 parse + lower，但**不写 kvspace**。它是自造代码的装载前闸门：LLM 生成或外部注入的 `.kv` 先过 vet，失败不污染 `/lib`。

## KVLANG_LIB 引导与 init 入口

`KVLANG_LIB` 引导 layout 与 run 各 lib 的 init。pkg **严格取自 `lib` 声明**（`lib X { … }` → `X`），不从文件名推导。layout 产出的 init 入口名列表即消费方驱动 init 的依据；tutorial 按约定跑 `test` 入口。
"## -> /lib/kvlang/规范/layout语义/layout流水线
}
