// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/读参与写参 {
	r##"# 读参与写参

| 参数 | 布局槽位 | 方向 |
|------|---------|------|
| **读参** | `[0,-1], [0,-2], …` | 调用方 → 被调方 |
| **写参** | `[0,+1], [0,+2], …` | 被调方 → 调用方 |

`-> result` 是**写参的跨帧路径映射**，不是返回值：runtime 将子帧写参直写调用方目标位置（见 [[调用与返回]]）。

调用时**写参 arity 必须全部匹配**（对齐 Go/Rust）：`f() -> s` 对多写参函数是错误；不需要的写参**必须**用 `_` 显式丢弃。
"## -> /lib/kvlang/规范/layout语义/读参与写参
}
