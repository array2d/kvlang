// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/for-in脱糖 {
	r##"# for … in 的脱糖

`for (x in iter)` 由 lower 脱糖为索引循环：初始化临时索引与长度槽，条件块自增并比较，体首取元素绑定迭代变量：

- 容器源（struct / stringkeymap）——长度用 `kv·listlen`，取元素用 `kv·listn` + `kv·get`。
- compact 数组源——长度用 `ndarray·numel`，取元素用 `xv·at`。

迭代源为裸标识符时原地遍历；为表达式（如数组字面量）时先物化到临时槽。迭代变量 `x` 是被写槽——`x` 命中读参名则拒绝（见 [[函数]] 读参只读）。
"## -> /lib/kvlang/规范/layout语义/for-in脱糖
}
