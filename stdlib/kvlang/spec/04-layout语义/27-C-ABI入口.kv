// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/C-ABI入口 {
	r##"# C ABI 入口

layout 能力经 cdylib 导出，第三方无需 fork 子进程即可布局（各入口都用 catch_unwind 兜住内部 panic，坏输入只返回 -1）：

| 符号 | 职责 |
|------|------|
| `kvlangLayoutFile(path, dsn, …)` | 读 `.kv` 文件 layout 进 kvspace |
| `kvlangLayoutCode(src, dsn, …)` | 内存源码串 layout 进 kvspace |
| `kvlangLayoutVet(src, …)` | 只校验（parse + lower），不写 kvspace |
| `kvlangLayoutFormat(src, …)` | 格式化（parse → 规范化源码），不写 kvspace |
| `kvlangLayoutDump(lib, dsn, …)` | 把 `/lib` 子树逆向重建为可运行 kvlang + 槽位注释 |
| `kvlangKindexprParse(kindexpr, out)` | 解析 XValue head 的 kindexpr（唯一事实源，供各消费方复用） |

成功返回 0（`entry_out` 为 init 入口名列表，`\n` 分隔），失败返回 -1（`err_out` 为错误）。`kvlangLayoutFile` 是 `kvlangLayoutCode` 的薄封装。
"## -> /lib/kvlang/规范/layout语义/C-ABI入口
}
