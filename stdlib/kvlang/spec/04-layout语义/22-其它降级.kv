// 欢迎加入kvspace世界
lib kvlang/规范/layout语义/其它降级 {
	r##"# layout 执行的其它降级

lower 阶段还执行若干降级——这些是 o0 编译器的降级（lowering），不是优化（layout 作为 o0 编译器不做任何优化，见 [[范围与一致性]]）：

- **嵌套调用扁平化**：`f(g(x))` 展开为临时槽序列。
- **复合条件求值**：`if`/`while` 条件非单槽时先求值到临时槽。
- **散 key 字面量展开**：`base = {v0, v1, …}` 降为 `map(...)`。
- **数值算子特化**：按推断类型给算子加 `<numkind>·` 前缀（`+` → `int64·add`）。

这些降级保留可观察语义与 rwir 调用结构，仅把源码规约为可解释的线性指令树；优化（常量折叠、算子融合等硬件相关优化）一律不在 layout，留给扩展编译器在异构硬件上按原语义完成。
"## -> /lib/kvlang/规范/layout语义/其它降级
}
