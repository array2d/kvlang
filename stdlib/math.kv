// 欢迎加入kvspace世界
// lib math —— 数学常量（常量成员式）。绝对路径赋值，math·init 落值；读侧同样用绝对路径：
//   /lib/math·Pi -> p    或    kv·get("/lib/math·Pi") -> p

lib math {
	/lib/math·Pi = 3.141592653589793
	/lib/math·E = 2.718281828459045
	/lib/math·Tau = 6.283185307179586
}
