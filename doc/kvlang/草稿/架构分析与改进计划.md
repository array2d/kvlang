# kvlang 架构分析与改进计划

> 分析时间：2026-07-10（v3，含 lexer 迁移与 ParseLine 重写后状态）
> 分析范围：全部 Go 源码（internal/ + cmd/，约 5000 行）
> 分析方法：逐文件通读，追踪调用链，检查死代码与逻辑一致性

---

## 一、整体层次结构

```
┌──────────────────────────────────────────────────────────────┐
│  cmd/kvlang         CLI 入口 (run / serve / load / vet / fmt) │
├──────────────────────────────────────────────────────────────┤
│  internal/parser    .kv 文本 → AST                           │
│  internal/lower     AST → 基本块（if/while → block+br）      │
├──────────────────────────────────────────────────────────────┤
│  internal/layoutcode  编译期写 KV + 运行时 HandleCall  ← 混合│
├──────────────────────────────────────────────────────────────┤
│  internal/kvcpu     执行引擎 (execute / control / sched)     │
│  internal/vtype     VType 命名空间分发接口                    │
│  internal/op/dispatch  后端路由 (op-plat / heap-plat)        │
│  internal/op/builtin   原生内建算子                           │
├──────────────────────────────────────────────────────────────┤
│  internal/vthread   VThread 状态管理                         │
│  internal/keytree   KV 路径常量中心                          │
│  internal/kvspace   KVSpace 接口（Redis 实现）               │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、做得好的地方

| 模块 | 评分 | 理由 |
|------|------|------|
| `keytree` | ⭐⭐⭐⭐⭐ | 全部路径常量集中，修改路径无需搜索全库 |
| `kvspace` 接口 | ⭐⭐⭐⭐⭐ | 10 个方法，干净；Redis/Mock 可互换 |
| `execute.go` 四路 switch | ⭐⭐⭐⭐⭐ | 全静态分发，零 KV 分类查询，注释精准 |
| `vtype` 命名空间 | ⭐⭐⭐⭐ | `tensor.*`/`str.*` 前缀分发，消灭了 `isFunctionCall` |
| `builtin` 注册模式 | ⭐⭐⭐⭐ | `Op` 接口 + registry map，可插拔 |
| `lower` pass | ⭐⭐⭐⭐ | 纯变换，无副作用，if/while → block+br 完整 |
| `op.Frame` | ⭐⭐⭐⭐ | 执行上下文干净，4 个字段 |
| `parser.ParseLine` | ⭐⭐⭐⭐ | 三种语法（前缀 / 中缀 / C 风格）均支持 |

---

## 三、问题清单

### 3.1 死代码

| 位置 | 内容 | 状态 |
|------|------|------|
| `internal/cache/` | 整包（KVCache、NewKVCache） | ✅ 已删 |
| `internal/op/instruction.go` | `DecodeFromCache` | ✅ 已删 |
| `internal/kvcpu/controlflow.go` | `handleBr`、`funcRoot`、`blockPC` | ✅ 已删 |
| `internal/lexer/` | 整包（227 行），从未被任何生产代码导入 | ✅ 已删（迁移+重写入 parser/） |
| `internal/keytree/src.go` | `SrcFuncLine`，零调用方 | ❌ 待删 |


### 3.2 正确性缺陷

#### 3.2.1 `mainWatcher` 创建 vthread 绕过 struct，Mode 字段丢失

```go
// serve.go:142 — mainWatcher（serve 模式）：Mode 字段缺失
kv.Set(base, `{"pc":"[0,0]","status":"init"}`)

// serve.go:24 — executeEntry（单次模式）：正确
st := vthread.VThread{PC: "[0,0]", Status: "init", Mode: "single"}
data, _ := json.Marshal(st)
kv.Set(keytree.VThread("run"), data)
```

P0 修复了 `vthread.Set` 保留 Mode，但 serve 模式的 vthread 从创建起 Mode 就是空串。
**修复**：mainWatcher 改用 `vthread.VThread` struct 序列化，或调 `vthread.CreateVThread`。

#### 3.2.2 `vthread.Set` 已修复（P0）

原问题：`Set(vtid, pc, status)` 重建 `VThread{}` 会丢弃 Mode 字段。
现状：已改为 Get + merge，Mode 字段正确保留。

---

### 3.3 逻辑冗余

#### 3.3.1 `strVType.Exec` 中的 `IsNativeOp` 检查永远为 false

```go
// vtype/str.go
func (s strVType) Exec(...) error {
    if builtin.IsNativeOp(inst.Opcode) { // ← 永远 false，死分支
        return builtin.Native(...)
    }
    // error
}
```

原因：`execute.go` dispatch 顺序是 `IsNativeOp`（case 2）在 `vtype.Lookup`（case 3）之前。
能到达 `strVType.Exec` 的 opcode 必然已通过 `!IsNativeOp` 过滤，所以 `IsNativeOp` 检查在此永远为 false。

#### 3.3.2 `parseParamList` / `parseParamListRaw` / `validate.go` ✅ 已清理

原问题：两个函数逻辑完全相同（约 50 行各一份），唯一差别是是否调 `stripQuotes`；
`validate.go` 中的 `validateRef` / `validateKeyRefs` 也仅在 `ParseLine` 内使用。

现状：ParseLine 重写为 `Tokenize()` + `parseInstFromTokens()`，上述函数全部删除：
- 删除：`parseParamList`、`parseParamListRaw`、`stripQuotes`、`isLiteral`
- 删除：`validate.go`（`validateRef`、`validateKeyRefs`、`isKeyRef`、`isString`）
- 新增：`collectParams`（11 行，职责单一）

---

### 3.4 视觉/格式问题

#### 3.4.1 `handleControl` switch 缩进错乱

```go
func handleControl(...) error {
    switch inst.Opcode {
    case op.OpCall:
        ...
        return nil
    case op.OpReturn:
        ...
        return nil
        case op.OpIf:     // ← 实际在 switch 顶层，但缩进像在 OpReturn body 里
            return If(...)
        case op.OpBr:
            return brToCall(...)
        default:
            return fmt.Errorf(...)
        }
    }
```

逻辑正确（Go 不看缩进），但阅读时极其误导。`gofmt` 可立即修复。

---

### 3.5 架构层面问题（P2，大改动）

#### 3.5.1 `layoutcode` 混合了编译期和运行时

同一个包（202 行）混合了两个完全不同的执行阶段：

```
编译期（load 时调用）：        运行时（execute 时调用）：
  WriteFunc                       HandleCall
  WriteBody                       HandleReturn
  RegisterBlocks                  copyFunc
```

修改执行模型时必须同时理解加载细节；修改加载格式时必须同时理解运行时状态机。
**理想拆分**：`internal/compiler/` ← 编译期；`HandleCall/HandleReturn` 移入 `kvcpu/`。

#### 3.5.2 `ast.Stmt.SetKV` 耦合存储层

`Stmt` 接口含 `SetKV(kv kvspace.KVSpace, prefix string, idx *int)`，导致：
- `IfStmt`、`ForStmt`、`WhileStmt`、`BreakStmt`、`ContinueStmt` 全部有空实现 `{}`
- AST 数据结构不得不 import `kvspace` 和 `keytree`

AST 是纯数据结构，不应知道存储层。`SetKV` 应由外部 compiler 包负责。

#### 3.5.3 `copyFunc` O(N×M) KV 操作——每次函数调用的主要开销

```
调用一个 10 指令的函数：
  kv.List(srcPrefix)            = 1  次 SMEMBERS
  每条指令：Get opcode + 10 reads + 10 writes = 21 次 GET
  写入 vthread：同上             = 最多 21 次 SET
  ──────────────────────────────────────────────────
  总计 ≈ 1 + 10×21 + 10×21 = 421 次 KV 操作，只为"复制函数体"
```

根本原因：执行模型是"把指令 copy 进 vthread 栈再执行"，而非"栈帧只存绑定，直接在 `/func/` 执行"。

**理想模型**：
```go
type CallFrame struct {
    FuncPath string            // 指向 /func/<pkg>/<name>
    PC       int               // 当前指令偏移
    Bindings map[string]string // 形参名 → vthread 路径
}
// 执行时直接读 /func/ 指令，用 Bindings 替换参数，无需 copy
```

#### 3.5.4 `/func/main` 一个 key 当两种用途

```
写入触发：{"entry":"pre_main","reads":[],"writes":[]}
mainWatcher 认领后删除，然后写入：{"vtid":"1","status":"executing"}
```

同一个 key 先是"启动信号"，后变成"执行状态"，语义混乱。应拆为两个 key：
- `/func/main`：只作启动触发，认领后永久删除
- `/vthread/<vtid>`：vthread 状态（已有）

---

## 四、各模块当前评分

| 模块 | 评分 | 主要问题 |
|------|------|----------|
| `keytree` | ⭐⭐⭐⭐⭐ | SrcFuncLine 未用（待删） |
| `kvspace` | ⭐⭐⭐⭐⭐ | 无 |
| `execute.go` | ⭐⭐⭐⭐⭐ | 无 |
| `vtype` | ⭐⭐⭐⭐ | strVType.Exec 有死分支 |
| `lower` | ⭐⭐⭐⭐ | 无重大问题 |
| `builtin` | ⭐⭐⭐⭐ | 无重大问题 |
| `op/dispatch` | ⭐⭐⭐⭐ | 无重大问题 |
| `parser` | ⭐⭐⭐⭐ | inBody 检测 O(N²) 有假阳性（小文件可忽略） |
| `kvcpu/controlflow` | ⭐⭐⭐ | handleControl 缩进混乱 |
| `layoutcode` | ⭐⭐ | 混合编译期/运行时；copyFunc 性能差 |
| `vthread` | ⭐⭐⭐ | mainWatcher 创建时 Mode 缺失 |
| `serve.go/mainWatcher` | ⭐⭐ | 轮询（1s ticker）；Mode 缺失；/func/main 双重用途 |
| `ast` | ⭐⭐ | SetKV 耦合存储层 |

---

## 五、改进优先级

### 🔴 P0 — 立即（改动极小，消除 bug 或死代码）

| # | 改动 | 文件 |
|---|------|------|
| 1 | 删 `keytree.SrcFuncLine` | keytree/src.go |
| 2 | 修 `mainWatcher` vthread 创建加 Mode 字段 | cmd/kvlang/serve.go |
| 3 | 删 `strVType.Exec` 中死分支（IsNativeOp 检查） | vtype/str.go |
| 4 | `gofmt` 修 `handleControl` 缩进 | kvcpu/controlflow.go |

### 🟠 P1 — 重要（中等改动，消除重复和结构混乱）

| # | 改动 | 文件 |
|---|------|------|
| 5 | `/func/main` 拆为触发 key + vthread 状态分离 | serve.go + keytree |

### 🟡 P2 — 架构重构（大改动）

| # | 改动 | 影响范围 |
|---|------|----------|
| 8 | 拆 `layoutcode`：compiler（编译期）vs kvcpu/frame（运行时） | layoutcode + kvcpu |
| 9 | `ast.Stmt` 去 `SetKV`，由 compiler 包负责序列化 | ast + compiler |
| 10 | CallFrame 代替 copyFunc：栈帧只存绑定，直接读 `/func/` 执行 | layoutcode + kvcpu |

---

## 六、已完成的修复记录

| 时间 | 内容 | commit |
|------|------|--------|
| 2026-07-10 | 删 cache 包、DecodeFromCache、handleBr/funcRoot/blockPC | 6942c9d |
| 2026-07-10 | 修 vthread.Set 保留 Mode 字段 | 6942c9d |
| 2026-07-10 | 删 layoutcode.parseSig，改用 parser.ParseSignature | 6942c9d |
| 2026-07-10 | 迁移 lexer → parser/lexer.go，修复 switch bug，重写 ParseLine 调用 Tokenize() | 3d9d09f |
| 2026-07-10 | 删 parser/validate.go（validateRef/validateKeyRefs/parseParamList/Raw 等 10 个函数） | 3d9d09f |
| 2026-07-10 | 修 ast.needsQuote：含特殊字符的参数值加引号，保证 String() round-trip 正确 | 3d9d09f |
