# hello: 验证整条 pipeline（parse→lower→load→run→print）能输出 hello world
# 层级: P0
# 规则: P0-smoke
# 期望输出:
#   hello = world

str.set("kvlangrun") -> ./term

def hello() -> () {
    str.set("world") -> ./msg
    print("hello =", ./msg)
}

hello() -> ()
