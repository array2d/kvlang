# max_eq: 验证 max(5, 5) = 5（相等输入边界）
# 层级: P1
# 规则: arith/max
# 等价类: E-相等 (max(5,5)=5)
# 期望输出:
#   C = 5

str.set("kvlangrun") -> ./term

def max_fn(A:int, B:int) -> (C:int) {
    max(A, B) -> ./C
}

max_fn(5, 5) -> ./c
print("C =", ./c)
