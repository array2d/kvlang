# to_bool_zero: 验证 bool(0) = false（零值→false）
# 层级: P1
# 规则: cast/bool
# 等价类: E-零→false (0→false)
# 期望输出:
#   C = false

str.set("kvlangrun") -> ./term

def bool_fn(A:int) -> (C:bool) {
    bool(A) -> ./C
}

bool_fn(0) -> ./c
print("C =", ./c)
