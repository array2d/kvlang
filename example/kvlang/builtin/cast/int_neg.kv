# int_neg: 验证 float→int 在负浮点数时向零截断（-3.7 → -3，不是 -4）
# 层级: P1
# 规则: cast/int
# 等价类: E-负数截断→向零 (-3.7→-3)
# 期望输出:
#   C = -3

str.set("kvlangrun") -> ./term

def int_fn(A:float) -> (C:int) {
    int(A) -> ./C
}

int_fn(-3.7) -> ./c
print("C =", ./c)
