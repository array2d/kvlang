# lt: 验证 < 在严格小于输入下产生 true（向后兼容测试）
# 层级: P1
# 规则: compare/<
# 等价类: H-严格小于→true (5<10)
# 期望输出:
#   C = true

str.set("kvlangrun") -> ./term

def native_lt(A:int, B:int) -> (C:bool) {
    ./C <- A < B
    print("C =", ./C)
}

native_lt(5, 10) -> ./out
