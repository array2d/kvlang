# eq: 验证 == 在相等输入下产生 true（向后兼容测试）
# 层级: P1
# 规则: compare/==
# 等价类: H-相等→true (5==5)
# 期望输出:
#   C = true

str.set("kvlangrun") -> ./term

def native_eq(A:int, B:int) -> (C:bool) {
    ./C <- A == B
    print("C =", ./C)
}

native_eq(5, 5) -> ./out
