# branch: 基本块条件分支
# 右侧冒号: label: { } — 定义处带 :，引用处不带

def 分支示例(flag:bool, X:int) -> (R:int) {
    X + 1 -> './a'

    if ('./flag') {
        './a' * 2 -> './b'
    } else {
        './a' * 3 -> './b'
    }

    './b' + 10 -> './R'
}

str.set("kvlangrun") -> './term'

分支示例(true, 5) -> './out'
