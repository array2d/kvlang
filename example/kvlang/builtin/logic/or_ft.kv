# or_ft: 验证 || 在 F||T 下产生 true
# 层级: P1
# 规则: logic/||
# 等价类: F||T → true
# 期望输出:
#   C = true

str.set("kvlangrun") -> ./term

def or_fn(A:bool, B:bool) -> (C:bool) {
    ./C <- A || B
}

or_fn(false, true) -> ./c
print("C =", ./c)
