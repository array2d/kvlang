# and: 验证 && 在 T&&F 下产生 false（向后兼容测试）
# 层级: P1
# 规则: logic/&&
# 等价类: T&&F → false
# 期望输出:
#   C = false

str.set("kvlangrun") -> ./term

def native_and(A:bool, B:bool) -> (C:bool) {
    ./C <- A && B
    print("C =", ./C)
}

native_and(true, false) -> ./out
