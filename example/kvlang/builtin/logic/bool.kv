# bool: 验证 bool() 在非零整数下产生 true（向后兼容测试）
# 层级: P1
# 规则: logic/bool
# 等价类: 非零→true (1)
# 期望输出:
#   C = true

str.set("kvlangrun") -> ./term

def native_bool(A:int) -> (C:bool) {
    bool(A) -> ./C
    print("C =", ./C)
}

native_bool(1) -> ./out
