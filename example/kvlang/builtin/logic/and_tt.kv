# and_tt: 验证 && 在 T&&T 下产生 true
# 层级: P1
# 规则: logic/&&
# 等价类: T&&T → true
# 期望输出:
#   C = true

str.set("kvlangrun") -> ./term

def and_fn(A:bool, B:bool) -> (C:bool) {
    ./C <- A && B
}

and_fn(true, true) -> ./c
print("C =", ./c)
