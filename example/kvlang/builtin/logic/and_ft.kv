# and_ft: 验证 && 在 F&&T 下产生 false
# 层级: P1
# 规则: logic/&&
# 等价类: F&&T → false
# 期望输出:
#   C = false

str.set("kvlangrun") -> ./term

def and_fn(A:bool, B:bool) -> (C:bool) {
    ./C <- A && B
}

and_fn(false, true) -> ./c
print("C =", ./c)
