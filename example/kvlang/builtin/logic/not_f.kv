# not_f: 验证 ! 在 !F 下产生 true
# 层级: P1
# 规则: logic/!
# 等价类: !F → true
# 期望输出:
#   C = true

str.set("kvlangrun") -> ./term

def not_fn(A:bool) -> (C:bool) {
    ./C <- !A
}

not_fn(false) -> ./c
print("C =", ./c)
