# 字符串基础：字面量、码点长度、+ 拼接
# 默认编码 char/utf32（定宽码点，可索引，len 返回码点数）
# 期望输出:
#   hello
#   5
#   kvlang
rwfunc main() -> () {
	s = "hello"
	println(s)
	println(string.len(s))
	t <- "kv" + "lang"
	println(t)
}
main()
