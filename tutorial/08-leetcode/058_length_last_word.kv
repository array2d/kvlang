# 058: Length of Last Word
# 来源: LeetCode #58
# 期望输出:
#   5
rwfunc last_word_len(s:[]char/utf32) -> () {
	n = string·len(s)  # = 等价于 <-
	n - 1 -> i
	count <- 0
	while (i >= 0) {
		c = string·char(s, i)  # = 等价于 <-
		c == " " -> sp

		if (sp) {
			i <- -1
		} else {
			count = count + 1  # = 等价于 <-
		}

		i - 1 -> i
	}
	println(count)
}

last_word_len("Hello World")
