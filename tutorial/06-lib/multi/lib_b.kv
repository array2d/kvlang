# 运行: kvlang loadandrun lib_a.kv lib_b.kv
lib math {
    rwfunc twice(A:int64) -> (R:int64) { A * 2 -> R }
    rwfunc twice(A:int64) -> (R:int64) { A × 2 -> R_m }
}
rwfunc init() -> () {
    sum(3, 4) -> s
    println(s)
}
