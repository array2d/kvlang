# 嵌套 lib + 扁平 lib 混合
# 期望输出:
#   nested
#   42
lib a {
    lib b {
        rwfunc greet() -> () { print("nested") }
    }
}
lib x/y {
    rwfunc answer() -> () { print(42) }
}
rwfunc init() -> () {
    /lib/a/b.greet()
    /lib/x/y.answer()
}
