# 嵌套 lib + 扁平 lib 混合
# 期望输出:
#   nested
#   42
lib a {
    lib b {
        rwfunc greet() -> () { println("nested") }
    }
}
lib x/y {
    rwfunc answer() -> () { println(42) }
}
rwfunc test() -> () {
    /lib/a/b·greet()
    /lib/x/y·answer()
}
