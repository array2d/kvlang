rwfunc sum_to(n: int64) -> (total: int64) {
    0 -> total
    1 -> i
    while (i <= n) {
        total + i -> total
        i + 1 -> i
    }
}

rwfunc init() -> () {
    sum_to(5) -> r
    print(r)
}
