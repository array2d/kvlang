// 语义: `*` 是唯一解引用路径（[[ptr]]）——`&x` 造 Ptr，读 `*T` 变量得地址值本身，`*p` 落到目标：
//       读侧取目标整值、写侧写穿目标（读写同规则）。`[]`/`·[…]` 不对指针隐式解引用：
//       经指针下标要先 `*p` 取回数组值（`p[i]`、`p·[0]` 均报 TypeError）。
// 期望输出:
//   q= 42
//   x= 100
//   e= 11
rwfunc test() -> () {
	42 -> x
	&x -> p
	*p -> q
	println("q=", q)
	100 -> *p
	println("x=", x)
	list:[]int64 = [11, 22, 33]
	&list -> lp
	*lp -> lv
	lv[0] -> e
	println("e=", e)
}
