# gcd: 欧几里得最大公约数（尾递归）
# 算法: gcd(48, 18) = 6
# 期望输出:
#   gcd = 6
def gcd(A:int, B:int) -> (R:int) {
	if (B == 0) {
		A + 0 -> R
	} else {
		rem <- A % B
		R = gcd(B, rem)  # = 等价于 <-
	}
}

gcd(48, 18) -> ans
print("gcd =", ans)
/last_gcd <- ans
