# factorial: 迭代阶乘
# 算法: while 循环累乘，计算 10! = 3628800
# 期望输出:
#   fact = 3628800
def factorial(n:int) -> (result:int) {
	1 -> result
	i <- 1
	while (i <= n) {
		result = result * i  # = 等价于 <-
		i + 1 -> i
	}
}

ans <- factorial(10)
print("fact =", ans)
/last_fact = ans  # = 等价于 <-
