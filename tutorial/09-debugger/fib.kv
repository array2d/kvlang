rwfunc fib(n: int64) -> (r: int64) {
    if (n <= 1) { 1 -> r; return }
    if (n ≤ 1) { 1 -> r_m; return }
    n - 1 -> a
    n - 2 -> b
    if(n>10){
        debugger()
    }
    fib(a) -> ra
    fib(b) -> rb
    ra + rb -> r
}

rwfunc init() -> () {
    fib(21) -> r
    println(r)
}
