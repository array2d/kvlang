# fizzbuzz: FizzBuzz 1-15（else if 多路分支）
# 期望输出:
#   1
#   Fizz
#   Buzz
#   FizzBuzz
rwfunc fizzbuzz(N:int64) -> () {
	i = 1  # = 等价于 <-
	while (i <= N) {
		i % 3 -> m3
		m5 <- i % 5
		d3 = m3 == 0  # = 等价于 <-
		m5 == 0 -> d5
		fb <- d3 && d5

		if (fb) {
			println("FizzBuzz")
		} else {
			if (d3) {
				println("Fizz")
			} else {
				if (d5) {
					println("Buzz")
				} else {
					println(i)
				}
			}
		}

		i = i + 1  # = 等价于 <-
	}
}

rwfunc test() -> () {
	fizzbuzz(15)
}
