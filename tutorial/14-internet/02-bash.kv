# internet/proc·exec 启动 bash：算术求值，取退出码
# 期望输出:
#   bash 42
#   exit= 0
rwfunc test() -> () {
	noenv = map()
	args = {"bash", "-c", "echo bash $((6*7))"}
	internet/proc·exec(args, noenv) -> code
	println("exit=", code)
}
