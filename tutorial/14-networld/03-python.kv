# networld/proc·exec 启动 python3：执行一行脚本，取退出码
# 期望输出:
#   python 1024
#   exit= 0
rwfunc test() -> () {
	noenv = map()
	args = {"python3", "-c", "print('python', 2**10)"}
	networld/proc·exec(args, noenv) -> code
	println("exit=", code)
}
