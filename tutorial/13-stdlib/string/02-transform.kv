# string 变换：reverse / repeat / upper / lower
# 期望输出:
#   olleh
#   ababab
#   HELLO WORLD
#   hello world
rwfunc test() -> () {
	println(string·reverse("hello"))
	println(string·repeat("ab", 3))
	println(string·upper("hello World"))
	println(string·lower("Hello WORLD"))
}
