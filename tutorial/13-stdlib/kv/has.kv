// kv·has：检测 KV 路径是否存在
// 期望输出:
//   has missing: false
//   has existing: true

rwfunc test() -> () {
	h1 <- kv·has("/missing_key")
	println("has missing:", h1)
	kv·set("/exists_key", "hello")
	h2 <- kv·has("/exists_key")
	println("has existing:", h2)
}
