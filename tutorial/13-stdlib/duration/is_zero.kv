// is_zero：判断 duration 是否为零
// 期望输出:
//   true
//   false
rwfunc test() -> () {
	time/duration·nanos(0) -> z
	time/duration·is_zero(z) -> r
	println(r)
	time/duration·seconds(1) -> one
	time/duration·is_zero(one) -> r2
	println(r2)
}
