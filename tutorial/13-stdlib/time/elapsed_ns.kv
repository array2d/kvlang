// time·elapsed_ns：从指定时刻到现在的纳秒数（确定 >= 0）
// 期望输出:
//   elapsed >= 0: true
// 自 t0 至今经过的纳秒数非负
rwfunc elapsed_nonneg(t0:time) -> (ok:bool) {
	time·elapsed_ns(t0) -> ns
	ns >= 0 -> ok
}

rwfunc test() -> () {
	time·now() -> t0
	elapsed_nonneg(t0) -> ok
	print("elapsed >= 0: ")
	println(ok)
}
