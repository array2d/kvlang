// 期望输出:
//   n= 3

// 列出 prefix 下的子键并计数
rwfunc count_children(prefix:[]char/utf32) -> (n:int64) {
	kvspace·list(prefix) -> names
	kvspace·listlen(names) -> n
}

rwfunc test() -> () {
	kvspace·mkindex("/tmp/kvtc/", 8)
	kvspace·set("/tmp/kvtc/a", 1)
	kvspace·set("/tmp/kvtc/bb", 2)
	kvspace·set("/tmp/kvtc/ccc", 3)
	count_children("/tmp/kvtc/") -> n
	println("n=", n)
}
