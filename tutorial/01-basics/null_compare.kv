# null == 0 按 int64(0) 处理（fix-017 方案 A，与 null==0 比较语义一致）
#
# 期望输出：
#   true
#   true
#   false
def main() -> () {
    null == 0 -> r1
    print(r1)
    null < 1 -> r2
    print(r2)
    null != 0 -> r3
    print(r3)
}
main()
