# 258: Add Digits — 数字根（反复各位相加直到一位数）
# 来源: LeetCode #258
# 期望输出:
#   2
def add_digits(n:int) -> () {
	while (n >= 10) {
		0 -> s
		n -> x
		while (x > 0) {
			x % 10 -> d
			s + d -> s
			x / 10 -> x
		}
		s -> n
	}
	print(n)
}

add_digits(38)
