# 258: Add Digits — 数字根（反复各位相加直到一位数）
# 来源: LeetCode #258
# 期望输出:
#   2
def add_digits(n:int) -> () {
	while (n >= 10) {
		s = 0  # = 等价于 <-
		n -> x
		while (x > 0) {
			d <- x % 10
			s = s + d  # = 等价于 <-
			x / 10 -> x
		}
		n <- s
	}
	print(n)
}

add_digits(38)
