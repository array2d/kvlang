# 172: Factorial Trailing Zeroes — n! 尾部零的个数
# 来源: LeetCode #172
# 期望输出:
#   1
def trailing(n:int) -> () {
	0 -> count
	while (n >= 5) {
		n / 5 -> n
		count + n -> count
	}
	print(count)
}

trailing(5)
