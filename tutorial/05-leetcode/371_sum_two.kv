# 371: Sum of Two Integers — 位运算加法
# 来源: LeetCode #371
# 期望输出:
#   5
def get_sum(a:int, b:int) -> () {
	while (b != 0) {
		carry = a & b  # = 等价于 <-
		carry << 1 -> c2
		a <- a ^ b
		b = c2  # = 等价于 <-
	}
	print(a)
}

get_sum(2, 3)
