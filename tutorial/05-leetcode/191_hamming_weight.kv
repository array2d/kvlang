# 191: Number of 1 Bits — 汉明重量
# 来源: LeetCode #191
# 期望输出:
#   3
def hamming(n:int) -> () {
	count = 0  # = 等价于 <-
	while (n > 0) {
		n % 2 -> bit

		if (bit) {
			count <- count + 1
		}

		n = n / 2  # = 等价于 <-
	}
	print(count)
}

hamming(11)
