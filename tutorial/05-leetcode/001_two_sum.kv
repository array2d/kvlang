# 001: Two Sum — 数组参数版
# 来源: LeetCode #1
# 期望输出:
#   [ 0 , 1 ]
#   [-1, -1]
def two_sum(nums, target:int) -> () {
	n <- len(nums)
	found = 0  # = 等价于 <-
	0 -> i
	while (i < n) {
		j <- 0
		while (j < n) {
			sk = i == j  # = 等价于 <-

			if (sk) {
				j + 1 -> j
			} else {
				x <- nums[i]
				y = nums[j]  # = 等价于 <-
				x + y -> s
				hit <- s == target

				if (hit) {
					print("[", i, ",", j, "]")
					found = 1  # = 等价于 <-
					n -> i
					j <- n
				} else {
					j = j + 1  # = 等价于 <-
				}
			}
		}
		i + 1 -> i
	}
	nf <- found == 0

	if (nf) {
		print("[-1, -1]")
	}
}

two_sum(array(2, 7, 11, 15), 9)
two_sum(array(1, 2, 3), 99)
